# Pace — architecture (systems reference)

This is the canonical detailed architecture reference for Pace — the per-system
bullet list that previously lived inline in the [repository AGENTS.md](https://github.com/HeyPace/pace/blob/main/AGENTS.md). It was
extracted to keep `AGENTS.md` a concise agent bootloader. The doctrine and the
high-level constellation diagram live in [`overview.md`](overview.md); per-file
responsibilities live in [`../development/key-files.md`](../development/key-files.md).

## App shape

- **App Type**: Menu-bar-first (`LSUIElement=true`) with no Dock icon. On MacBooks with a camera housing, the always-available Living Notch signal and quick panel are joined by one on-demand resizable Command Center window. Displays without a reported housing use a conventional status item and standalone panel; Pace never draws a synthetic center notch.
- **iPad companion target**: `PacePad` is a full-screen iPadOS 26 input/output surface in the same Xcode project. It owns Bonjour discovery, authenticated reconnect, tap-to-talk recording, local system TTS, the companion face, low-rate local presence detection, and requested-only camera still capture. It owns no planner, durable memory, Mac inspection, tool execution, or interruption policy.
- **Framework**: SwiftUI (macOS native) with AppKit bridging for onboarding, the Command Center window, menu-bar panel, hardware-derived notch overlay, and cursor overlay
- **Pattern**: MVVM with `@StateObject` / `@Published` state management
- **Concurrency**: `@MainActor` isolation, async/await throughout
- **Analytics**: local-only no-op/timing-safe hooks via `PaceAnalytics.swift`; no cloud analytics SDK is linked.

## iPad companion transport

- `PaceCompanionServer` is a thin Mac adapter over the existing conversation,
  local audio transcription, privacy-pinned vision, world observation, and
  proactivity pipelines. It advertises `_pace-companion._tcp` and never creates
  a second planner or memory store.
- `PaceShared` defines versioned messages and a length-prefixed frame containing
  bounded JSON control data plus optional AAC/JPEG bytes. Heartbeats and a
  bounded message-ID window handle connection health and replayed frames;
  shared policy also validates semantic payloads, stale sessions, privacy gates,
  heartbeat expiry, and bounded reconnect timing.
- Pairing uses a six-digit, in-memory code only to establish TLS 1.3 PSK
  transport. The Mac then issues a random 256-bit credential stored in each
  device's Keychain. Reconnect PSKs and a session HMAC derive from that
  credential; no secret is stored in UserDefaults or logged.
- The iPad sends ordinary utterance audio and semantic presence events. JPEG
  bytes are sent only in response to an expiring `camera_frame_request` created
  by the conservative explicit physical-scene request parser. Pause or camera
  disable discards queued presence, cancels pending still continuations, and is
  re-checked before either side sends or accepts camera media. Temporary Mac
  audio files are removed after local transcription and requested images are
  never persisted.
- If the Mac's selected planner crosses the on-device boundary, the existing
  audit/tint behavior remains authoritative and the iPad processing and
  speaking states become amber for the same turn.

## Planner

- **Planner**: main screen/action planning uses the local OpenAI-compatible LM Studio reasoner. The current source default for both planner and fallback VLM is one resident `qwen/qwen3.5-4b` multimodal model, eliminating the missing-model and model-swap stalls caused by the previous separate Gemma/UI-Venus defaults. Pure-knowledge turns use the bundled in-process MLX planner when enabled; otherwise they use LM Studio's native `/api/v1/chat` stream with `reasoning: off` and fall back before first output to the OpenAI-compatible route with both supported non-thinking hints. Structured action turns stay on Chat Completions for JSON-schema decoding. `<think>…</think>` stripping remains defensive. The bundled path reuses a complete Hugging Face CLI cache and runs one real inference at launch; an early user turn awaits that same warmup task instead of racing a second model load. The 2026-08-27 qualification scored 16/19 on the legacy planner fixtures and 7/8 on production v10 action fixtures, with the focused five-turn set averaging 878 ms. Gemma 3 12B remains an optional quality-over-memory planner and `qwen/qwen3-30b-a3b` remains the stronger 48 GB swap. Apple Foundation Models is scoped to classification and structured extraction rather than world-knowledge answers. See [`../development/info-plist-switches.md`](../development/info-plist-switches.md). No cloud LLM. **First-launch tier default:** on a brand-new install with no `pace.planner.tier.` UserDefaults state at all, `PacePlannerTierStore.loadConfiguration()` checks `SystemLanguageModel.default.availability` and resolves to `.appleFoundationModels` when Apple Intelligence is available, `.local` otherwise. Existing persisted tier choices remain authoritative.
- **Planner (`BuddyPlannerClient`)**: the active planner is chosen by the user in Settings → Planner via `PacePlannerTier` (Local / CLI bridge / CLI direct / Direct API / Apple FM). Default local model is `qwen/qwen3.5-4b`. `BuddyPlannerClientFactory.makeDefault()` dispatches on tier: `LocalPlannerClient` for `.local` and as a fallback for missing/invalid configs; `AppleFoundationModelsPlannerClient` for `.appleFoundationModels`; `CloudBridgePlannerClient` for `.cliBridge`; `PaceLocalCLIPlannerClient` for `.cliDirect`; and `DirectAPIPlannerClient` for `.directAPI`. The same `CompanionSystemPrompt` flows through every tier. Direct-API keys live in macOS Keychain via `PaceKeychainStore`. Any off-device tier tints the capsule amber and fails loud unless the user explicitly enables local fallback. The main action planner is decode-constrained to the v10 `{spokenText,intent,payload}` envelope; invalid structured output retries without prompt cache. Eval coverage lives in `evals/fm-fixtures-actions/` and is scored on decoded actions.
  - **`.cliDirect` general brain (OpenSpec `openspec/changes/archive/2026-07-12-codex-general-brain`)**: a user-selectable tier that direct-spawns the user's already-authenticated `codex` (default) or `claude` CLI — no Node bridge, only the CLI on PATH — as the brain for ALL turns (the same `PaceLocalCLIPlannerClient` the `.research` lane already uses). Upstream sub-selection persists via `PacePlannerTierStore` key `pace.planner.tier.cliDirect.upstream` (`PaceLocalCLIUpstream`, default `.codex`). Off-device, so it inherits the FULL cloud-bridge safety contract via a *separate* transport-aware consent: `PaceCloudBridgeConsent.hasAcceptedDirectSpawnConsent()` + a 24-hour direct-spawn soak (`canRunDirectSpawnTurn(now:)`) — accepting the Node-bridge consent does NOT auto-grant the direct-spawn path. A `.cliDirect` turn tints the capsule amber (`isOffDeviceTurnInFlight`, keyed on `plannerClientForThisTurn is PaceLocalCLIPlannerClient`), writes a `PaceAPIAuditLog` entry (`subsystem: "planner.cliDirect"`, `target: &lt;codex|claude&gt;`) so the Privacy dashboard flips off "0 bytes → X KB to codex", and FAILS LOUD via `PaceFailureNarrator` on CLI error (no silent local fallback beyond the existing opt-in). Missing-binary preflight (`PaceLocalCLIPlannerClient.isUpstreamBinaryOnPath`) surfaces a plain-language "needs `codex` on PATH" hint before the turn. **Codex is the phone-a-friend default:** the `.research` lane defaults to Codex on fresh installs (`PaceResearchTierStore.defaultCLIBridgeUpstream = .codex`, `defaultCLIBridgeModel = ""` so Codex uses its own authenticated model; persisted upstreams honored for existing users), and cron/scheduled fires (`CompanionManager+Lifecycle` `executeTaskCallback`) route through a Codex direct-spawn brain ONLY when direct-spawn consent + 24h soak are satisfied (`BuddyPlannerClientFactory.cronTaskBrainDecision`, sharing `cliDirectDispatchDecision`) — otherwise they fall back to the local planner, so an unattended background fire never silently goes off-device (amber forced for the Codex path). The planner brain is also selectable from Settings → Models via a **RAM-aware brain picker** folded into `PaceModelMemoryBudget`'s budget section (`PaceBundledModelsSettingsTab.memoryBudgetSection`): picking a cloud/Apple-FM brain frees the local-planner RAM and the section live-updates the per-model breakdown, fits verdict, and largest-VLM-that-fits recommendation. Both the Planner tab and the Models picker route through the shared `CompanionManager.selectPlannerTierWithConsent`.

## Voice, vision, TTS

- **Speech-to-Text**: Apple **`SFSpeechRecognizer`** (on-device, `requiresOnDeviceRecognition=true`) is the default active provider — instant, no model download. `TranscriptionProvider=whisperKit` is accepted as a scaffolded local provider selection, but falls back to Apple Speech until the real WhisperKit streaming bridge lands. All cloud STT providers have been removed.
- **Text-to-Speech**: two on-device conformers of `BuddyTTSClient`. The dependable default is **`LocalTTSClient`** (`AVSpeechSynthesizer`, Premium > Enhanced > compact `LocalTTSVoiceIdentifier`) because it has no sidecar or model-readiness dependency. `TTSProvider=localServer` opts into **`LocalServerTTSClient`**, backed by a loopback Kokoro-82M `/v1/audio/speech` service with per-utterance Apple fallback and a 30-second outage memo. Cloud TTS has been removed.
- **Local Vision-Language Model (optional)**: LM Studio at `http://127.0.0.1:1234/v1` (OpenAI-compatible). The default `qwen/qwen3.5-4b` model is shared with the local planner, so only one model stays resident. Accessibility and native OCR run first; the VLM is the fallback for screens those native paths cannot expose. Planner/VLM HTTP roots are loopback-only. Pure-Q&A typed turns do not prewarm or call the VLM.
- **Screen Capture**: ScreenCaptureKit (macOS 14.2+), multi-monitor support
- **Voice Input**: Push-to-talk via `AVAudioEngine` + pluggable transcription-provider layer. System-wide keyboard shortcut via listen-only CGEvent tap.
- **Voice Input UI**: `PaceNativeInterfaceModel` maps ready, listening, understanding, approval, acting, speaking, completed, blocked, and failed into one semantic state language. `PaceSignalView` renders that language across onboarding, the real notch, and the quick panel; off-device planner activity overrides the active signal to amber. Timeline rendering exists only for active states, and Reduce Motion uses static or short crossfade presentations.
- **Cursor**: Codex-style arrow (`CodexArrowShape`) with linear-gradient fill, white highlight stroke, dual shadow.
- **Element Pointing**: the planner embeds `[POINT:x,y:label:screenN]` tags in its response. The overlay parses these, maps coordinates to the correct monitor, and animates the cursor along a bezier arc to the target.

## Memory

- **Two-tier in-context memory**: every planner call carries the last K=4 turns verbatim (`PaceThreadMemory.verbatimWindow()`) AND a rolling summary of everything older (`injectionPrefix()`, rendered into the system prompt as `&lt;conversation_so_far&gt;...&lt;/conversation_so_far&gt;`). The summary is refreshed by a detached Apple FM call after each turn via `PaceThreadSummarizer`; the user-facing path never blocks on summarization. Race-safety lives in a monotonic `summaryVersion` snapshot captured before each detached call, so out-of-order arrivals are dropped at `applySummaryUpdate`. In-session a 20-minute idle threshold still drops the live state (or Settings → Memory → Reset thread). The conversation now SURVIVES quit/relaunch: `PaceThreadMemory.snapshot(now:)`/`restore(from:)` are pure value-type accessors and `PaceThreadMemoryStore` persists a single on-device JSON (`~/Library/Application Support/Pace/thread-memory.json`, atomic write) after every turn/summary/reset; `CompanionManager.start()` rehydrates it. Policy is "resume always, until reset" — no staleness expiry; the file is wiped only on an explicit thread reset or when thread memory is disabled. The summary is still never journaled to `paceHistory` (only session id + lifecycle events are). Durable facts go through episodic memory instead.
- **Always-On Companion Mode (default off)**: typed provenance-bearing `PaceWorldObservation` evidence uses bounded atomic persistence, current-state/last-seen/change/presence queries, confidence decay, correction supersession, deterministic memory promotion, and a dedicated retrieval source. `PaceCompanionModeController` exposes off/starting/observing/interpreting/paused/degraded/privacy-blocked; `PaceCompanionInterventionPolicy` defaults to silent memory and keeps card/speech opt-ins separate. Settings → Companion and menu-bar indicators expose source consent, active capture, retention/storage, readiness, pause, and clear. App lifecycle starts explicitly enabled ambient/watch sources plus a production low-rate AVFoundation/Vision camera client that emits only non-identifying ephemeral person tracks and conservatively matches user-taught, locally persisted Vision feature prints across coarse camera zones. Teaching is an explicit centered-object capture; no photo is persisted. Ambient voice uses a bundled local pre-STT Core ML gate (`PaceWakeWordClassifier`, exact labels `hey_pace` and `background`); missing or malformed assets fail closed before Speech.framework is reachable. Accepted wakes hand off to the bounded push-to-talk conversation path. Silent cards and spoken interventions are separately default-off and wired through the intervention policy; speech additionally passes the existing live restraint/cooldown path. Routine promotion requires at least three unique supporting observations. On 2026-07-13 the owner directed "push through," accepting the remaining unmeasured live/hardware and manual `Cmd+R` risk without representing those checks or documented thresholds as passed. See `docs/product/companion-mode-privacy.md`, `docs/product/companion-mode-dogfood.md`, and `docs/product/pace-wake-word-classifier.md`.
- **Outcome/feedback telemetry (Slices 1-2)**: `PaceInterventionOutcomeModel.swift` adds a typed `PaceInterventionOutcomeRecord` (`interventionKind` `actionApproval`/`reversibleMutationUndo`, outcome `accepted`/`dismissed`/`undone`) answering "what happened after Pace suggested or acted" — the plan's Gap #3, `openspec/changes/2026-09-13-add-outcome-feedback-telemetry`. `PaceInterventionOutcomeStore` caps retention per `interventionKind` (not per subject — approval/undo summaries are near-unique, so a per-subject cap wouldn't bound anything) and exposes a read-only `outcomeCounts(forInterventionKind:)` query; `PaceInterventionOutcomePersistenceStore.swift` persists it, restored at `CompanionManager.start()` and saved after every record. The two owner-confirmed producers (`CompanionManager+OutcomeFeedbackTelemetry.swift`) tap existing decision points with zero new capture: `requestUserApprovalForActionPlan`'s `NSAlert` decision records `accepted`/`dismissed`, and `triggerUndoLastMutation`'s undo-banner tap records `undone`, correlated to the specific reversible action via a new `mostRecentReversibleActionIdentifier` minted in `noteReversibleActionExecuted`. Not yet surfaced to any ranking/learning layer.
- **Activity/goal model (Slices 1-2)**: `PaceActivityGoalModel.swift` adds a typed, provenance-bearing `PaceActivityObservation` (`observed`/`inferred`/`userStated`/`authorizedTask`) and a derived `PaceActiveGoalState` (current subject or `unknown`, with supporting/contradicting observation ids) answering "what is the user's active outcome right now" — a question none of the existing memory tiers answer. `PaceActivityGoalStore` enforces a 200-observation-per-subject retention cap; a correction is an observation carrying `supersedesObservationId` rather than a separate tombstone list. `PaceActivityGoalPersistenceStore.swift` persists it following the `PaceMemoryStore`/`PaceThreadMemoryStore` atomic-JSON pattern, restored at `CompanionManager.start()` and saved after every observation. The one Slice 2 producer (`openspec/changes/2026-09-13-add-activity-goal-model`, owner-confirmed 2026-09-13 to stay app-transition-only for now) taps `PaceAppUsageTracker.handleApplicationActivated` through a new optional `onActivityObserved` closure — the exact signal the app-usage journal already receives, gated by the same `appUsageHistory` toggle, no new capture/permission/model call. Not yet surfaced to any card/UI/ranking layer (Gap #2/#5/#6 build on top of this later).

## Actions, tools, integrations

- **Action Layer (agent mode)**: the planner should prefer `&lt;tool_calls&gt;` JSON blocks where the outer array is sequential steps and each inner array is a parallel group. Tool metadata lives in `PaceToolRegistry` so prompt docs, aliases, and risk labels share one source of truth; the local registry validates at app startup so schema examples, aliases, and enum coverage cannot silently drift. Legacy tags are still accepted: `[CLICK:x,y]`, `[DOUBLE_CLICK:x,y]`, `[TYPE:text]`, `[KEY:cmd+s]`, `[SCROLL:up:3]`, `[OPEN_APP:Safari]`, `[OPEN_URL:https://example.com]`, `[MUSIC:play]`, `[VOLUME:up:2]`, `[BRIGHTNESS:down:3]`, `[CALENDAR:today]`, and `[REMINDER:send invoice]`. `PaceActionTagParser` extracts them; `CompanionManager` asks for user approval only for higher-risk non-undoable or external actions when the `Approve Risky Actions` preference is on, and the approval alert defaults to Cancel. Routine local actions such as click, scroll, window snap, app/URL open, media, volume, brightness, clipboard read, and undo can execute without the popup; blocking preflight issues still surface before execution. Keyboard input (`type`, `pressKey`, and text-field edits) requires the same approval popup — unlike a click landing on an already-described target, a keystroke or typed string can't be distinguished from typing into a password field, a send box, or a payment form at the action-type level (Phase 2H remediation; see `docs/PHASE_2H_APPROVAL_ENFORCEMENT_REMEDIATION.md`). `PaceActionApproval` keeps the allow/cancel decision testable; `PaceActionExecutor` posts events after TTS playback starts. Single-clicks try `PaceAXTargeter` first (AX-tree press), falling back to CGEvent. AX set-value edits are logged in a session-local mutation log and can be restored with `Undo.last` / "undo that". App launch/URLs use `NSWorkspace`; volume, brightness, media, and clipboard reads use local macOS APIs; Calendar/Reminders use EventKit for reads and creation. Mail recipient names can resolve through Contacts before composing a draft. Finder/Notes/Mail/Things/Shortcuts/Messages have first-pass local integrations. `download_file` downloads a user-named http(s) URL into `~/Downloads` with URL validation, filename sanitization, and Finder-style collision suffixes — always approval-gated. Gated by `EnableActions=true` in Info.plist. Runtime smoke hooks are disabled unless `PACE_ENABLE_SMOKE_HOOKS=1`.
- **Trust surfaces (visible undo + reply replay + failure narration)**: every reversible mutation (`createNote`, `appendNote`, `createReminder`, `createCalendarEvent`, `composeMail`, `createThingsToDo`, `runShortcut`, `downloadFile`, `recordFlow`/`runFlow`, `setTextValue`/`editSelectedText`, `openMessages` with body, and generic `mcp`) raises a 5-second floating `PaceUndoBanner` next to the cursor; tapping it submits `Undo.last` through the executor. After every assistant turn finishes speaking, the notch panel renders a 30-second reply-replay button driven by the same post-processed string that flowed through TTS — replay does NOT re-stream the planner. Plain-language failures (planner offline, missing permission, click missed, sidecar TTS offline, MCP server not configured, cloud-bridge upstream error) are composed deterministically by `PaceFailureNarrator` (no LLM call) and routed through `PaceRestraintGate.decide(...)` so failure speech stays silent during a Zoom/active call. Reversibility uses the same set as `PaceActionApprovalPolicy.actionIsReversibleMutation` returns true for, and the click-missed narrator is wired to the all-fail click observation in `PaceActionExecutor`.
- **Typed automation catalog**: Versioned manifests under `Resources/automations/` contain canonical Pace tool calls, provenance, execution mode, required preferences, invocation examples, and ordered step groups. The indexed 17-entry starter library covers Notes templates, Calendar reads, timers, Finder locations, current-window layouts, bounded media control, weekly review, and end-of-day reset. `PaceAutomationDefinitionLibrary` validates every manifest at startup against `PaceToolRegistry`, rejects input injection, network/download, destructive, nested-flow, and opaque Shortcut capabilities, and calls `PaceAutomationCompiler` to reject partial parses. The compiler feeds the existing `PaceActionTagParser`, so typed automations reuse normal preflight, approval, executor, observation, and audit behavior. `PaceAutomationCatalog` builds a non-persisted view over typed automations (`deterministicLocal`), bounded Pace Programs (`deterministicProgram`), recorded flows (`deterministicReplay`), skills (`plannerGrounded`), and installed Shortcuts (`externalOpaque`). Explicit list/run commands still resolve locally by unique exact normalized name. Ordinary completed text then uses exact authored invocation aliases or the local Nomic/Apple embedding chain; token overlap never authorizes execution. A unique semantic score above both the absolute threshold and winner-margin gate dispatches directly. Close candidates can be resolved by a fresh typed-output Apple Foundation Models session that runs fully on-device, may choose only from the embedding shortlist, validates an opaque candidate identifier, and can decline or ask for clarification. Unavailable models and weak or failed matches preserve the original planner fallthrough. Catalog metadata is never added to any off-device planner prompt or conversation memory.
- **Programmable teachable skills**: `PaceProgramDefinition` is a versioned declarative graph stored under `~/Library/Application Support/Pace/programs/`. It supports ordered typed-action steps, conditions over one pre-run snapshot (weekday, local hour, or frontmost bundle identifier), and literal repetition. The whole graph—including inactive branches—is validated before save, discovery, and each run. Fixed limits are nesting depth 4, 50 source nodes, repeat count 10, and 50 worst-case expanded action steps. Programs cannot contain Lua, JavaScript, shell, imports, network/file APIs, raw Accessibility operations, or branches on action output. The selected graph flattens into a transient typed definition and reuses `PaceAutomationCompiler` plus normal preflight, approval, executor, observation, and audit behavior; no model runs after catalog selection. “Teach a skill …” and “Create an automation …” share a privacy-pinned local authoring ladder: fixed typed automation first, bounded Pace Program second, planner-grounded `.skill.md` last. The local model is an authoring aid, never runtime authority.
- **Teachable skills**: `.skill.md` skills (`PaceSkillLoader`) are the natural-language sibling of typed automations, Pace Programs, and recorded flows — a numbered step list the planner **re-grounds each run** (intent tier), rather than compiling canonical tool calls (typed/program tier) or replaying recorded AX steps verbatim (flows, pixel tier). Bundled skills live in `Resources/skills/&lt;slug&gt;.skill.md`; user skills in `~/Library/Application Support/Pace/skills/`. **Teachable by telling** (PRD `docs/product/prds/teachable-skills.md`): explicit teach/create transcripts use the shared representation ladder above; Settings → Skills → "Teach a skill" remains an explicit planner-grounded prose form. `skillStructuringSystemPrompt` + `skillFromStructuredJSON` use the privacy-pinned LOCAL text planner, with `structureSkillDeterministically` as a no-model fallback. Stored triggers are active catalog invocation phrases. `PaceSkillLoader.save`/`deleteUserSkill`/`listUserSkills` use the atomic temp-file+rename pattern and reuse `PaceFlowStore.slug(for:)`. `PaceSkillsView` surfaces prose skills and deterministic programs in "Your skills." Fully on-device — authored reusable work never leaves the Mac.
- **MCP Integration Layer**: broad external integrations should use OSS Model Context Protocol servers instead of being rebuilt inside Pace. `PaceMCPClient` loads stdio server definitions from `~/.config/pace/mcp-servers.json` or `~/.pace/mcp-servers.json`, accepts common `mcpServers` config shape, runs MCP `initialize` + `tools/call`, and returns observations through the same approval/result loop. Planner MCP calls use either `{"tool":"mcp","server":"altic","name":"notes_create","arguments":{...}}` or `{"tool":"notes_search","server":"altic","query":"..."}` for server-native names. Missing configured server names are surfaced by `PaceToolPreflight` before approval. The bridge speaks newline-delimited JSON-RPC and is validated end-to-end by `PaceMCPClientIntegrationTests` against the in-repo `scripts/mcp-fixture-server.py`; `mcp-servers.example.json` curates popular OSS servers (filesystem, fetch, github, applescript) as ready-made connectors. **Bundled one-tap catalog** (Settings → MCP): `PaceMCPServerCatalog` ships a fixed four-server list (filesystem, fetch, applescript, composio) installable with a click; github/slack/linear now route through the composio OAuth bridge (`PaceMCPServerCatalog.supersededBySlug`). `PaceMCPCatalogInstaller.install/uninstall(_:into:)` performs an atomic JSON merge into `mcp-servers.json` — temp file + rename — that preserves every user-added entry. Catalog is bundled with each Pace release; no remote fetch of the server list at runtime.

## Observation, journals, proactivity

- **Watch mode**: `PaceScreenWatchModeController` is the explicit watch-loop primitive. It samples screenshots, uses `PaceScreenImageDiffer`, and emits typed events only when a screen has meaningful visual change (`majorScreenChange`, `contentUpdate`, `focusedRegionChange`). The companion panel exposes a `Watch Mode` toggle, and explicit voice commands such as "watch my screen" / "stop watching" route through `PaceWatchModeCommandParser` before the planner. Watch events also journal into the local retrieval index (`PaceScreenWatchJournal`, source `screenWatchHistory`) so "what did I do today?" questions answer from local history.
- **Time understanding (journals)**: two passive retrieval sources power Dayflow-style recall. The screen watch journal records watch-mode events (timestamp, change category, frontmost app, cached VLM description when fresh) into day-per-screen documents; the app usage journal (`PaceAppUsageTracker` + `PaceAppUsageJournal`, source `appUsageHistory`) tracks foreground app minutes and switch counts through permission-free NSWorkspace notifications even when watch mode is off. Both keep 7 days, dedupe/cap entries, rehydrate across restarts, and honor per-source enable/clear in Settings. A third recall source, `researchHistory` (`PaceResearchJournal`), captures each research-lane turn as a day-bucketed `{question, answer}` document (30-day/100-entry retention, same-day dedupe) — recorded by a strictly `isResearchTurn`-guarded, non-blocking, fire-once hook at the agent-loop turn finalization (question = original transcript, answer = final spoken text) — so "what did I research about X?" answers from history and the turns are browsable in Settings → Memory → Past research.
- **Proactive features (opt-in)**: the menu-bar app exposes a small set of proactive surfaces gated by `PaceRestraintGate` (active-call check, proactive cooldown, intent confidence). Posture watch (`PacePostureMonitor`), focus-fatigue nudges, calendar pre-meeting nudges, watch-mode observation nudges, and the daily weekday **morning brief** (`PaceMorningTriageScheduler` + `PaceMorningBriefBuilder`) all default OFF and become inert until the user enables them in Settings. When restraint says "stay quiet" the morning brief is parked on a panel card so the user never misses it. **Opportunity ranking (Slices 1-2)**: `PaceOpportunityRanking.swift` (`openspec/changes/2026-09-13-add-opportunity-ranking`) adds the coalesce/per-category-cooldown/rank/cap layer `openspec/specs/proactive-companion-policy/spec.md`'s accepted "resists repetition" requirement already commits to but that didn't exist before this — previously `PaceProactiveNudgeOrchestrator`'s three generators (focus-fatigue, calendar-pre-meeting, watch-mode-observation) ran with zero cross-generator coordination beyond the single global cooldown in `PaceRestraintContext`. `PaceOpportunity`'s failable initializer makes a gate-refused candidate unrepresentable, so ranking can only narrow what `PaceRestraintGate` already approved, never widen it. Owner-confirmed wiring (2026-09-13): `PaceProactiveNudgeOrchestrator.start`/`setGeneratorEnabled` now wrap each generator's `emit`/`queueForLater` closures (its own `identifier` becomes the ranking `category`) — zero changes to `PaceProactiveNudges.swift` or the three generator files themselves, all pre-existing generator/orchestrator tests pass unmodified. Acceptance-history stays honestly `.unavailable` (no nudge-level accept/dismiss producer exists yet). **Slice 3** (`PaceProactiveNudgeOrchestrator.mostRecentRankingResult`) retains the latest `rank(...)` result — a read-only evidence query, overwritten every tick — consumed by the Gap #5 Now surface projection below; still no UI displays it.

- **Gap #5 surface projection foundation**: `PaceSurfaceProjection.swift` + `CompanionManager+SurfaceProjection.swift` (`docs/current/plans/autonomous-companion-consolidation.md`, "5. Product-surface consolidation") add pure, I/O-free `PaceNowSurfaceProjection` / `PaceWorkingSurfaceProjection` / `PaceMemorySurfaceProjection` functions that reshape three already-shipped stores — `PaceActivityGoalStore` + the opportunity-ranking Slice 3 result (Now), `PaceBackgroundAgentRunner` (Working — previously unconsumed by any UI), `PaceEpisodicFactStore` (Memory) — into read-only surface state, with no new persistence and no new source of truth. Privacy-reviewed per field: Working excludes `PaceBackgroundAgentTask.resultSummary`/`.failed(String)` literal text (only `hasResult: Bool` + a state enum), and Memory independently re-applies `PaceEpisodicSensitiveTopics` filtering rather than trusting the caller. This is plumbing only — the actual Now/Working/Memory UI remains a genuine, pending UX decision; see `docs/product/consolidation-dogfood.md`.
- **On-device meeting notes**: `PaceMeetingModeController` (in `PaceSystemAudioCapture.swift`) captures mic + system audio as two separate tracks via `PaceMeetingAudioRecorder`, segments them into attributed turns via `PaceMeetingTurnSegmenter` (Accelerate RMS + hysteresis + echo trimming), transcribes each turn on-device via `PaceAudioFileTranscriber.transcribeAudioFileSegmented` (WhisperKit → Apple Speech fallback), synthesizes structured notes (summary + action items + decisions) via `PaceMeetingNotesBuilder` using a privacy-pinned LOCAL planner (`BuddyPlannerClientFactory.makeLocalOnlyPlannerForPrivacyPinnedFeatures()` — never the CLI bridge or Direct API tier, regardless of the user's tier selection), and journals them via `PaceMeetingNotesJournal` into the `meetingNotes` retrieval source so "what did we decide in standup?" answers from local history. Fully on-device — no cloud STT, no cloud LLM. The wedge against Granola/Otter/Fireflies (all cloud). Recording files use `.part` → atomic rename with RIFF-header crash repair. See PRD `docs/product/prds/on-device-meeting-notes.md` and ADR `decisions/0001-meeting-audio-capture.md`. **Adaptive note profiles** (meetily-informed, Pace-native — see `openspec/changes/archive/2026-07-12-adaptive-meeting-notes`): notes are shaped by a selectable `PaceMeetingNoteProfile` (bundled `Resources/meeting-note-profiles/&lt;slug&gt;.json` — general/standup/one-on-one — plus user overrides in `~/Library/Application Support/Pace/meeting-note-profiles/`, loaded/validated by `PaceMeetingNoteProfileLibrary`). The `general` profile renders byte-for-byte identical to the legacy prompt (compat anchor, default). Selection precedence: explicit selection (voice command or per-meeting panel pick) → non-general default preference → optional local inference (a silent on-device classify call, default OFF) → general. **Voice-triggered**: "start my one-on-one recording" / "record this standup" starts a meeting with that profile pinned — `PaceMeetingModeCommandParser` routes the profile name before the recording starts.
- **Deeplinks and typed-turn scheduling (`pace://`)**: external launchers (Raycast, Shortcuts) can trigger Pace via `pace://listen`, `pace://chat?text=...`, `pace://watch?enabled=true|false`, and `pace://panel`. `PaceDeepLinkParser` is a pure reject-on-ambiguity parser (500-char chat cap). Listen remains start-only while a turn is active. Typed chat submitted during an active turn enters `PaceChatTurnQueue` and drains FIFO as each complete turn returns to idle; the panel keeps optimistic user rows in order, shows the pending count, and can clear pending work without cancelling the active turn. Pace deliberately does not run complete turns in parallel because TTS, approval, conversation memory, and Mac actions share one authoritative agent loop and parallel completion would interleave user-visible effects. Each active turn owns a generation lease plus separately tracked routing and response tasks, so explicit cancellation during intent or automation discovery cannot later install stale speech or UI state. Chat/listen turns still run the normal intent/approval pipeline, so a deeplink can do nothing the user's own voice couldn't. URL types registered via `CFBundleURLTypes`; handled in `CompanionAppDelegate.application(_:open:)` with pre-launch buffering.

## Loop, routing, latency

- **Intent routing**: `PaceIntentClassifier` is a tiny rule-based local classifier. It routes chitchat to a canned response, pure-knowledge to a text-only planner path, and labels screen-read, tool-action, and phone-large-model turns without paying model latency. On eligible Macs, only unmatched `.unknown` turns use the Apple Foundation Models classifier as a local fallback before the full pipeline.
- **Plan-act-observe loop**: `CompanionManager.sendTranscriptToPlannerWithScreenshot` runs a multi-step loop. Each step re-screenshots, re-invokes the VLM (heuristic permitting) and the planner, executes grouped tool calls/actions, returns tool observations to the next planner step, and continues until the planner emits `[DONE]`, emits no tool calls/action tags, or hits `AgentMaxSteps` (default 8).
- **Speculative planner race (first step only)**: on a screen-action / screen-description FIRST step, when `CompanionManager.speculativeRaceShouldFire` passes (race toggle on, local VLM configured, Apple FM available), `performFirstStepSpeculativePlannerRace` runs `PaceSpeculativePlannerRace.raceSpeculative`: the in-process Apple FM "lite" planner (transcript + thread-memory only, NO VLM) races the full VLM-fed planner. The full path's VLM+OCR+AX prep is deferred into the race's lazy builder so it runs concurrently with lite — lite produces audio in ~150 ms while a cold VLM (2–3 s) is still resolving. Whichever streams first wins TTS; the full path supersedes the lite stream if its first token lands within 500 ms / 60 spoken chars (`prepareForSupersedingStreamWithinTurn` resets the pipeline so the new stream replays cleanly). **Action parsing ALWAYS uses the full planner's complete text, never lite** — the lite path is spoken-feedback only and can't emit a real action; when lite wins the audio, the lite text becomes the spoken/bubble/memory string so `flushFinal` can't double-speak. `bothFailed` routes to `PaceFailureNarrator`. Gate false (and every multi-step turn) keeps the single-planner path byte-identical. Toggle: `PaceUserPreferencesStore.enableSpeculativePlannerRace` (default ON, opt-out in Settings → Planner).
- **Walking avatar (optional)**: `PaceAvatarOverlay` paints a small SwiftUI character at the bottom of the cursor screen in its own tiny `NSPanel`, but it is not attached at app launch. The default always-visible surface is the menu-bar/notch capsule.

## Key architecture decisions

**Native Surface Pattern**: `PaceOnboardingWindowManager` hosts a versioned, resumable first-run state machine that migrates the legacy Boolean completion flag, consolidates live permission setup, and reaches one typed or spoken request through `submitChatTranscriptFromDeepLink`. A truthful response or completed action contracts the in-window signal toward a notch-shaped anchor before the window closes; failure and blocked outcomes do not play success. `PaceMenuBarOverlayManager` derives the closed Living Notch frame from `NSScreen.safeAreaInsets` and the two auxiliary top areas and matches the physical housing exactly at idle. Hover and active states add compact horizontal wings at menu-bar height, with state left of the reserved housing width and signal right of it. A deliberate click resizes that same clipped `NSPanel` equally left, right, and downward, embedding `PacePanelChatView` inside the surface instead of opening a visually joined second window. `MenuBarPanelManager` routes show, hide, shortcut, deep-link, and smoke-test requests into the integrated notch only while reliable physical-notch geometry exists. On notchless or external-only display setups, `PaceMenuBarOverlayManager` creates a standard `NSStatusItem`, and the same requests open the standalone panel anchored below it. The geometry and fallback choice recalculate after wake and display changes; Pace never draws a synthetic housing. `PaceWindowLayering` keeps the optional screen-edge activity glow below both the system menu bar and Living Notch while deliberate teaching annotations remain topmost; the glow defaults off and is transparent while idle. Delayed onboarding transitions are cancellable and generation-guarded so stale work cannot advance a newer scene.

**Command Center Pattern**: `PaceMainWindowManager` owns one normal resizable `NSWindow` for every durable management surface. `PaceCommandCenterRouter` groups primary destinations under Use Pace, Activity & Privacy, Customize, and Help, while optional integrations and developer controls use one searchable Advanced disclosure. It honors exact requested routes and restores the last valid selection. `PaceSettingsWindowManager` is a compatibility facade into the same window rather than a second host. The notch panel remains a present-turn surface; its gear routes to Customize → General. Pace still runs as `LSUIElement=true`, and the Command Center explicitly activates the app only when opened.

**Cursor Overlay**: A full-screen transparent `NSPanel` hosts the blue cursor companion. It's non-activating, joins all Spaces, and never steals focus. Cursor flight, response text placement, and pointing animations render in this overlay via SwiftUI through `NSHostingView`; listening/thinking animation lives in the notch bar.

**Global Push-To-Talk Shortcut**: Background push-to-talk uses a listen-only `CGEvent` tap instead of an AppKit global monitor so modifier-based shortcuts like `ctrl + option` are detected more reliably while the app is running in the background.

**Transient Cursor Mode**: When "Show Pace" is off, pressing the hotkey fades in the cursor overlay for the duration of the interaction (recording → response → TTS → optional pointing), then fades it out automatically after 1 second of inactivity.

## Q — secure local agent runtime

- **What it is**: a parallel, security-governed autonomous agent runtime (`leanring-buddy/q-runtime/` plus root-level `Q*.swift` security primitives) developed on the `q-secure-foundation` branch alongside the main Pace product surfaces documented above. It is a separate execution pipeline from the `PaceActionExecutor`/`PaceToolRegistry` agent loop described under "Actions, tools, integrations" — the two do not currently share a tool registry, though Q's own approval flow does now share Pace's notch/panel HUD (see below).
- **Security model**: `QCapabilityModel.swift` defines a Level 0-4 risk hierarchy (read-only → safe local action → user-approval-required → high-risk → hard-blocked). Every action request passes through `QResourceGuard` (path denylist/jail) and `QPermissionGate` (capability grants, fail-closed default-deny, context-taint-forced approval) before `QExecutionService` dispatches it; `QAuditLogger` records every decision with secret redaction.
- **Planning & execution loop**: `QModelPlanParser` treats local-model output as untrusted data, validating it against a single-source-of-truth capability allowlist before it becomes an authoritative `QPlan`. `QPlanExecutor` executes steps sequentially with independent per-step authorization and mandatory empirical verification (`QActionVerifier`) before advancing — a step is never marked complete on a model's say-so. `QGoalEvaluator` and `QReplanController` close the loop: goal satisfaction requires verified runtime evidence, replanning is capped (default 2 attempts) with loop detection, and a security-blocked plan can never be replanned around.
- **Decision Engine & Task Decomposer (Phase 2A)**: `QCoreRuntime.submitIntent` computes a `QDeterministicDecisionEngine.decide(for:)` result (`QDecisionPlan` — task type, complexity, decomposition decision, reasoning-step budget, conceptual model strategy, verification/provenance requirement, bounded resource envelope, uncertainty) once, before planning begins, from `task.intent`/`task.context.isTainted` only — pure, synchronous, deterministic, no model call. Sits strictly ABOVE `QModelRouter`: it never selects a concrete backend (`QModelRouter`'s `priorityOrder`/`selectBestBackend` are untouched) and never grants permission/resource/egress authority (`QPermissionGate`/`QResourceGuard`/`QEgressBroker` are untouched). A provider that additionally conforms to `QDecisionContextAwareModelProvider` (`QModelRouter` does) receives the `QDecisionPlan` as a few bounded, non-sensitive lines appended to the existing planning prompt — advisory only; the returned `QPlan` still passes through the unmodified schema/permission/resource/verification pipeline. `QDeterministicTaskDecomposer` produces a bounded, deterministic, content-agnostic subtask scaffold (`QTaskDecompositionResult`) when `QDecisionPlan.decompositionDecision` is `.recommended`/`.required`; since `QPlan`/`QPlanStep` has no parent/child subtask concept yet, `.recommended` decomposition is computed and recorded (`task.decision.evaluated` lifecycle event) but never executed, and `.required` decomposition (`.criticalHighRisk` tasks) FAILS CLOSED — the task is refused before the planner is ever invoked, rather than inventing a subtask execution mechanism or bypassing plan validation. No separate phase report exists for this one — the full design rationale (including why `.required` decomposition fails closed rather than inventing an execution mechanism) lives inline in the `QDecisionEngine.swift`/`QTaskDecomposer.swift`/`QDecisionAwarePlanning.swift` source file headers.
- **Bounded Multi-Model Orchestration (Phase 2B)**: `QDeterministicModelOrchestrator` (`QModelOrchestrator.swift`) sits strictly ABOVE `QModelRouter`, exactly like the Decision Engine sits above the planner — `QCoreRuntime.submitIntent` prefers it (via the new `QModelCandidateAwareProvider` protocol, which `QModelRouter` conforms to) over the plain Phase 2A.4 path when the configured model provider supports it. Given a `QDecisionPlan`, it asks the router which backends are registered (`candidateBackends()`) and available (`candidateDescriptor(for:)`), then attempts a bounded number of them via `QModelRouter.generateStructuredPlan(...,preferredBackend:)` (a new, additive overload — every attempt still runs the unmodified `routeInference` availability/egress checks and the unmodified `QModelPlanParser` schema/risk validation for whichever backend it targets). Racing is bounded to `maxConcurrentCandidates = 2` and is only attempted for `.moderate`/`.complex` complexity with `.medium`/`.high` uncertainty — never for `.trivial`/`.simple` (nothing to compare) or `.critical` (which never reaches the orchestrator at all: `.criticalHighRisk` tasks always hit Phase 2A.4's `.required` decomposition fail-closed gate first). The orchestrator refuses to ever attempt a non-local candidate itself (defense in depth beyond `QEgressBroker`'s own loopback-only authorization), never selects a backend on its own independent authority, never executes a `QPlan` action, and never grants permission/resource/egress authority — every winning plan still flows through the exact same `QPlanExecutor → QPermissionGate → QResourceGuard → QActionVerifier` pipeline any other plan already does. Early exit is purely objective (Swift structured concurrency's own `withThrowingTaskGroup`/`group.cancelAll()` — first schema-valid plan wins, losers are cooperatively cancelled and their outcome recorded as `.cancelled`, never abandoned) — never response length, lexical overlap, formatting, or a fabricated confidence score; every attempt's outcome is one of 8 typed `QModelAttemptOutcome` cases (`accepted`/`rejected`/`invalid`/`timedOut`/`cancelled`/`unavailable`, plus `verificationFailed`/`needsVerification` — explicit Phase 2C extension points this phase's own orchestrator never produces). Two new `QTaskLifecycleEventType` cases (`task.model.attempt`, `task.model.orchestration.completed`) record only bounded identity/outcome-label/duration/count metadata — never raw model output or prompt text — following the exact same advisory/observability-only precedent `task.decision.evaluated` (Phase 2A.4) already established. Full design rationale lives inline in `QModelOrchestrationContracts.swift`/`QModelOrchestrator.swift`'s own file headers.
- **Evidence, Verification, Critic & Synthesis (Phase 2C)**: strictly AFTER the Phase 2B orchestrator and after execution — it *evaluates* results and is never an execution path. `QEvidencePipeline` (`QEvidencePipeline.swift`) turns model results, Phase 2B candidate attempts, user-provided facts, execution observations, and (injected, untrusted) retrieval drafts into a bounded, in-memory `QEvidencePool` (`QEvidencePool.swift`), then runs `QIndependentVerificationService` → `QEvidenceCritic` → `QEvidenceSynthesizer` and returns a `QEvidencePipelineResult` plus audit-safe `QEvidenceOutcomeMetadata`. The execution chain is unchanged (model → proposed `QPlan` → `QModelPlanSchema` → capability registry → authoritative risk → `QPermissionGate` → `QResourceGuard` → execution → observation → `QActionVerifier`); no Phase 2C type references any authority type (a static test asserts this), so none can grant permission, egress, resource, approval, execution, capability, or risk authority. `QCoreRuntime.submitIntent` runs the pipeline observe-only after each goal evaluation and records one `task.evidence.evaluated` lifecycle event (enums and counts only); its result is never consulted to decide anything.
  - **Trust boundary.** Source kinds are never collapsed into one "trusted" state: `userProvided`, `modelGenerated`, `retrievedExternal`, `executionObserved`, `deterministicCheck`. Kind and provenance must agree (a model/retrieved item can never carry trusted provenance; the pool rejects mismatches rather than coercing). Claim trust is a ladder computed only by the pool: `untrusted` (only models assert it) → `observed` (one non-model source) → `corroborated` (≥2 *distinct* non-model sources, no conflict) → `independentlyVerified` (an execution-evidence or deterministic-check verifier confirmed it). Model repetition — one model or many — a source that "looks authoritative", certainty language, and citations never raise trust. Citations give traceability only; a citation to a nonexistent item is dropped and counted.
  - **Claims & contradictions.** A claim is a concise `subject: value` proposition (`QClaimProposition`) with deterministic ID, source evidence IDs, derived status, verification requirement and contradiction state (`consistent`/`conflicting`/`unresolved`). No chain-of-thought is requested or stored. Conflicting claims are all preserved and marked; a conflict is settled only when exactly one value is established by model-independent evidence (an execution/deterministic observation or an independently verified claim) and every other-valued claim was contradicted by verification — never by model confidence, recency, or repetition.
  - **Verification.** Independent of the producer: execution evidence in the pool, registered deterministic checks, and — advisory only — an independent local model. A model may not verify its own claim (refused by the service and re-enforced by the pool); an independent-model verdict is recorded as `unresolved`, never `verified`/`contradicted`; disagreeing verifiers leave the claim `unresolved`. Result types: `verified`/`contradicted`/`unresolved`/`unavailable`/`notRequired`. The pool's requirement (`QVerificationRequirement`) is fixed at construction and forced to `independentVerification` for `.criticalHighRisk`/`.critical` plans (`QEvidenceRequirementPolicy`); `executionEvidence` needs an execution basis, `independentVerification` an execution or deterministic basis.
  - **Critic.** `QDeterministicEvidenceCritic` (one pass) reports unsupported claims, missing evidence, contradictions, reasoning gaps (citation that does not assert the claim), verification failures, unsupported certainty, and instruction-like evidence. Findings carry no action/permission/approval/verification-override field; critic output is untrusted until `QCriticValidator` drops findings with invented references, redacts and caps detail text, deduplicates, and caps the count. Findings only add caveats — they never change status either way. No numeric quality score.
  - **Synthesis.** Synthesis selects and labels pool claims; statement text is rendered from the claim (never from a synthesizer), each statement cites its claim's evidence IDs, and dispositions/status/caveats are re-derived by `QSynthesisPolicy` — a draft that proposes a stronger label or status is overruled (counted in `draftViolationCount`), an omitted claim (least of all one side of a conflict) is restored. Status: `sufficient`/`partial`/`contradictory`/`insufficient`/`unavailable`; zero or unverifiable evidence is reported as such, never as certainty. A lost collection/verification/critic stage caps a result at `partial`.
  - **Privacy.** The pool never stores raw content: items carry a SHA-256 hash, length, flags and bounded/redacted metadata; claims are bounded and credential-screened (a credential-shaped line is dropped, never stored redacted); provenance keeps its category but drops URL/path locators. Only `QEvidenceOutcomeMetadata` (plus its completeness enum and two opaque ID wrappers) is `Codable`, pinned by a test; pool/items/claims/findings/results are deliberately not, so they cannot be persisted by accident. No new logging system — the lifecycle event reuses the existing durable event store.
  - **Prompt injection.** Retrieved and model text is DATA. Instruction-shaped lines are flagged, skipped (never claims), and surfaced as an `instructionLikeEvidenceIgnored` caveat. The scanner is a flagging aid, not the defense: the defense is structural — no evidence text can reach permissions, egress, strategy, model authority, execution, or approval.
  - **Bounds.** `QEvidenceLimits`: 64 items, 64 claims (8 per item), 64 verification calls (≤3 backends per claim, sequential), 1 critic pass (≤32 findings), 1 synthesis attempt, no retries. Every stage has a timeout, is cancellable, and is failure-isolated (`QEvidenceStageState`); all async work is structured (nothing detached), and cancellation returns a partial result with no synthesis.
  - **Phase 2D/2E extension points.** `QEvidenceOutcomeMetadata` (candidate backends, per-candidate verification state, task type, synthesis/verification/critic stage state, evidence completeness, contradiction counts, user-correction flag, resource outcome) is the only structure intended to leave the pipeline. Candidate → attempt → result (Phase 2B) stays a separate lineage from evidence → claim → verification → synthesis; `QEvidenceOrigin` is the sole bridge. A router-backed independent judge (`QIndependentModelJudge`), a model-backed critic/synthesizer, and a retrieval collector are injection seams only — none is implemented, and any would inherit the same validation and gain no authority. Design rationale lives inline in the `QEvidence*.swift` file headers.
- **Model Capability Memory & Outcome Learning (Phase 2D/2E)**: lets future routing learn from real, verified outcomes without ever becoming a security authority. `Outcome → Evidence → Capability Observation → Capability Memory → Routing Recommendation → Model Orchestrator/Router → the existing, unchanged security pipeline`. Learning runs strictly AFTER execution and verification have decided; it cannot execute, approve, widen a permission, lower a risk, weaken verification, or touch a resource/egress limit, and no Phase 2D/2E type references any of those authorities (a static test asserts this, and that no model call, network, process, or second database exists in the new code).
  - **Observation model.** `QModelCapabilityObservation` (`QModelCapabilityContracts.swift`) records only things actually observed: task type, complexity, backend (never a model name as evidence), strategy, attempt outcome, verification, evidence completeness, contradiction, resource outcome, execution result, a real measured latency, explicit user feedback, timestamp. There is no self-rated capability, benchmark, probability, response length, or lexical/format signal. The `outcome` (`success`/`failure`/`partial`/`cancelled`/`timedOut`/`denied`/`verificationFailed`/`unresolved`/`correctedByUser`) is never accepted from a producer: `QOutcomeClassifier` derives it from the authoritative facts (negative evidence beats positive; success needs a satisfied goal AND positive/not-required verification), and the store re-derives and rejects any mismatch — a model "claiming" success while verification failed is unrepresentable.
  - **No fake scores.** `QModelCapabilityProfile` is typed counts per (task type, complexity, backend) — verified successes, verification failures, failures, partials, unresolved, verification-unavailable, timeouts, cancellations, denials, resource failures, unresolved contradictions, user corrections/confirmations, sample and distinct-task counts, and an exact mean of measured latencies. No floating-point field exists (tested). Cancellation (e.g. a race loser), denial (policy) and unresolved/unavailable are never counted as adverse.
  - **Persistence.** Two new tables (`q_model_capability_observations`, `q_model_capability_meta`) inside the EXISTING `QDurableTaskStore` (same SQLite file, WAL connection, and lock — `QModelCapabilityStore.swift`), not a second database or concurrency authority. Validate → dedupe → cap → insert → prune run in one `BEGIN IMMEDIATE` transaction (rolled back on any failure); identity is a deterministic hash so replays are `.duplicate` and a same-identity/different-facts report is `.conflictingDuplicate` (first stands). Reads skip and count rows that are corrupt, tampered (id/outcome re-validated), or of an unknown schema version — never trusting or deleting them. A newer on-disk schema, or an unreadable schema marker, leaves the data completely untouched (reads empty, writes refused). Existing task/plan/event tables are never modified. Crash/restart recovery and un-checkpointed-WAL recovery are tested. Bootstrap backs memory with the default on-disk store (in-memory when a custom path is supplied).
  - **Retention & bounds.** Sliding window of 90 days (this IS the decay policy: no weighting), 200 observations per key, 5,000 total, and at most 3 non-feedback observations per (task, backend) so one task cannot flood a model's history. Measured on the target laptop: ~0.5 ms per write, ~6 ms per profile read at the cap, ~2.7 MB on disk at 5,000 rows. No background loop and no extra model call exists for learning.
  - **Privacy.** Every column is an enum raw value, deterministic identifier, measured integer, or timestamp — there is no free-text column, so no prompt, response, screen text, OCR, typed text, credential, URL, or document body can be persisted (tested by reading the raw table after a real run). Only `QEvidenceOutcomeMetadata` (Phase 2C, enums/counts) is consumed; the evidence pool and raw model output never reach this layer. Lifecycle events (`task.model.routing.advised`, `task.outcome.learned`) carry counts and enum values only.
  - **Outcome learning (2E).** `QOutcomeLearningService` turns Phase 2C metadata + Phase 2B attempts + the authoritative `QGoalEvaluationState` into observations: the executed candidate carries the task-level result, other attempts only their own attempt-level facts. `QCoreRuntime` records the FIRST goal evaluation per task (later replans are not orchestrated candidates), each observation timestamped with the attempt's measured finish time and validated against the caller's clock. Phase 2C's metadata is task-level, so any contradicted claim makes verification `contradicted` (conservative: negative evidence can only make learning more cautious). Explicit user feedback (`QCoreRuntime.recordUserFeedback`) is one more evidence source; it is never inferred from silence/length/sentiment, is refused for a task with no observed outcome, must match the observed task's key, and is monotone (a correction supersedes a confirmation, never the reverse). No UI calls it yet.
  - **Poisoning defense.** No producer-supplied outcome; typed inputs only (no model text reaches learning); deterministic identity + idempotent store; per-(task, backend)/per-key/global caps; per-backend keys (one model's history never leaks into another's); future/expired timestamps, impossible latencies (rejected or clamped to 24 h), malformed identifiers and inconsistent feedback are refused; a candidate needs ≥5 DISTINCT observed tasks before it can be ranked, so repeated identical observations have bounded influence.
  - **Routing recommendation boundary.** `QModelCapabilityMemory.advise` returns a `QModelRoutingRecommendation` (an ordering plus the counts that explain it). The ranking rule compares exact integer fractions (verified successes / samples, then adverse / samples) only among candidates with enough distinct tasks; candidates without enough evidence keep their slot (a new model is neither promoted nor penalised). `QDeterministicModelOrchestrator(routingAdvisor:)` applies it AFTER its own registered+local+available filter and re-validates it as a permutation of those vetted candidates, so advice can neither add, drop, nor substitute a candidate — a non-local candidate cannot be reached, and every attempt still runs `QModelRouter`'s unchanged checks, `QModelPlanParser` validation, and Permission → Resource Guard → execution → verification. With no memory configured (the default for `QCoreRuntime`) behaviour is identical to Phase 2C.
  - **Extension points.** A per-task-type routing UI/diagnostic over `allProfiles`, an explicit feedback UI calling `recordUserFeedback`, learning on the `resumeTask` path, and per-candidate (rather than task-level) verification attribution are all deferred; none is needed for the guarantees above.
- **Verified Response Path — first slice (Phase 3)**: `Evidence → Verification → Verified Response Assembly → optional verified-memory write-back`. This slice is DETERMINISTIC and evidence-bound: it adds no model call, no model-backed paraphrase, and no scoring; candidate-level attribution and an independent model judge are deliberately deferred (the second slice adds the structured answer path below). It is informational only — assembly and write-back can never execute, approve, authorize, or widen anything, and "verified" ≠ "authorized" (no Phase 3 type references any authority type; a static test asserts this).
  - **Verified response.** `QVerifiedResponseAssembler` (`QVerifiedResponseAssembler.swift`) projects the existing `QEvidencePool` into a `QVerifiedResponse` (`QVerifiedResponseContracts.swift`): ordered statement REFERENCES (claim ID, evidence IDs, the pool's own trust/verification/contradiction standing, source kind, write-back eligibility), contradictions, fixed-vocabulary caveats, completeness, uncertainty, the pool's verification requirement, and provenance. It is Codable/Equatable and carries NO statement text — text lives only in the pool and is joined back at render time (`QRenderedResponse`, deliberately not Codable). There is one trust system: the assembler never computes trust; standing/status/caveats/uncertainty come from `QSynthesisPolicy` re-run against the pool. A statement whose claim or evidence references are empty or unresolvable is dropped and counted (fail closed); no evidence yields `insufficient` and an explicit "no evidence-backed statements" line — never prose. Critic findings only add a caveat; citations, repetition, authority language, and model confidence have no input path. Critical/high-risk work keeps its mandatory `independentVerification` (recorded from the pool). Unresolved and contradicted claims stay unresolved/contradicted and both sides of a conflict render unless the existing verification system actually settled it.
  - **Deterministic rendering.** `QVerifiedResponseRenderer` renders fixed labels per standing (`[VERIFIED]`, `[OBSERVED — not independently verified]`, `[UNVERIFIED — not established]`, `[UNRESOLVED]`, `[CONTRADICTED]`), the claim's own `subject: value`, its evidence IDs, conflict lines, and fixed caveat sentences. Same input → same lines (versioned by `renderingVersion`). No paraphrase, no prose scoring (no length/overlap/formatting/confidence).
  - **Memory provenance fix (anti-laundering).** Before this slice `recordTaskCompletion` stored model-generated summaries as `trusted:system` and `queryContext` returned bare strings, so unverified text could re-enter as "trusted". Now new completion records are labelled `untrusted:tool:model_summary`, and the additive `QProvenanceAwareMemoryProvider.queryContextItems` reports what each record IS: legacy rows (including any `trusted:*`-labelled row and any `task_completion:*` row) are conservatively `.unverified`; the user's own recorded intent is `.userStatement`; only a structured verified proposition is `.priorVerifiedProposition` — a HISTORICAL standing with no "currently verified" case. Legacy rows are never migrated into trusted status. When memory becomes evidence (`QMemoryContextItem.asEvidenceDraft`) it enters the pool as untrusted retrieved data (observed at most), so retrieval can never upgrade trust and a retrieved fact must be re-verified. The existing `queryContext` API is unchanged (the planner prompt path is untouched).
  - **Opt-in verified-memory write-back (default OFF).** `QVerifiedMemoryWriteBackConfiguration` defaults to disabled (there is no existing user-facing configuration surface for Q runtime features, so this is the minimal internal contract); `QCoreRuntime` takes an OPTIONAL `verifiedResponse:` configuration that defaults to `nil` (nothing assembled or written — identical to Phase 2E). A disabled writer never calls the store. Only a proposition the pool INDEPENDENTLY verified (execution-evidence or deterministic-check basis, requirement satisfied, no conflict, resolvable evidence) and that passes the URL / credential / instruction-shape screen is eligible; observed, corroborated, unverified, unresolved, and contradicted claims can never be written. `QVerifiedMemoryWriter` does not trust the Codable response: it re-derives eligibility from the pool and requires the response and pool to describe the same task.
  - **Persistence.** One new table (`verified_propositions`, plus a schema-version meta table) inside the EXISTING `QSQLiteMemoryStore` (same SQLite file, WAL connection, and lock — `QVerifiedMemoryStore.swift`); no second database. Each row is the bounded proposition (`subject: value`, ≤200 chars — the only free text stored) plus structured provenance: originating task, claim ID, supporting evidence IDs (the claim's own evidence plus the execution evidence that verified it), verification state, trust, verification basis, source kind, requirement, eligibility, timestamp, and a deterministic identity (hash of subject + normalized value). Writes are one `BEGIN IMMEDIATE` transaction and idempotent (a repeat, even from a later task, is a `.duplicate`; the first provenance stands). Bounded: 5 per subject, 2,000 total, oldest pruned. Rows are re-validated on every read (tampered id, an unverified row claiming verification, malformed evidence IDs, future time, unknown schema version → skipped and counted, never trusted or deleted); a newer/unreadable schema is left untouched. No prompt, model response, OCR/screen text, document body, credential, or URL is ever stored. Neither durable store restricts file permissions today (only the audit log does); this slice changes no file mode, and a test pins that.
  - **Runtime.** The integration is additive, at the existing evidence boundary: when configured, `QCoreRuntime` assembles the response from a pool built after goal evaluation, keeps the latest 32 rendered responses in memory (`verifiedResponse(forTask:)`), and records a content-free `task.response.assembled` event. The task summary, state, permissions, egress, resources, and completion are untouched; the existing `generateGroundedSummary` path is unchanged (deferred). Unless the structured answer path below is enabled, the pool holds only goal-evaluation/execution observations (no model claims), so nothing is independently verified and enabling write-back writes nothing — write-back cannot manufacture facts.
  - **Structured answer path (second slice).** Closes the gap that the Evidence Pool could accept model claims but nothing ever asked a model for any. `QStructuredAnswer.swift`: for a NON-EXECUTION task (simpleQA/reasoning/research/planning/creative — never execution, coding, `criticalHighRisk`, or critical complexity) `QCoreRuntime` makes ONE bounded, claims-only local model call (`subject: value` lines, ≤256 tokens, temperature 0, single-line truncated task text and fixed rules only — no memory, evidence, or plan in the prompt; timeout-bounded, cancellation-cooperative) through the router's unchanged `routeInference` choke point (local-only availability/egress checks; no cloud fallback, no backend selection of its own). It is ADDITIVE and OBSERVE-ONLY and default OFF (`QStructuredAnswerConfiguration` inside the already-optional `QVerifiedResponseConfiguration`, and additive `QStructuredAnswerProvider` conformance — providers without it are unaffected): it runs once per task, after execution and goal evaluation have decided, and never alters the user-visible summary, task outcome, or any authority. The reply enters the pool as `modelGenerated` evidence in a SEPARATE pipeline run from the one whose metadata feeds outcome learning, so unverified model claims can never change what capability memory learns (tested: the learned outcome is identical with and without it). Model claims stay `untrusted` unless independent execution/deterministic evidence verifies them — for a pure question there usually is none, so the response honestly renders them UNVERIFIED, and they are never write-back eligible. Every failure (no local backend, refusal, timeout, cancellation, empty/malformed output) degrades to "no answer claims", recorded as an enum state (`obtained`/`empty`/`unavailable`/`timedOut`/`cancelled`/`notEligible`/`providerUnsupported`/`notConfigured`) in the `task.response.assembled` payload with the answer claim count — never the text. The added latency when enabled is at most the configured timeout (default 10 s, max 60 s); no planner/candidate calls are added.
  - **Extension points.** Memory/file evidence collectors, candidate-level attribution into 2E (the structured answer is a single call, not per-candidate), an independent model judge, and swapping the user-visible summary for the rendered response are all deferred.
- **Durability & recovery**: `QDurableTaskStore` (SQLite WAL) persists task/plan/event state so a crash mid-execution can resume. `QTaskRecoveryManager` re-validates persisted state as untrusted on restart and performs observation-first verification of any in-flight step before ever re-executing a physical action. `QAgentBudget` bounds every task by max steps/replans/duration/consecutive failures/model attempts, enforced on both the initial run and any resumed execution.
- **Controlled mutating actions (Level 2/3)**: `QApprovalCoordinator` resolves `QPermissionGate`'s `.requireApproval` outcomes into single-use, execution-identity-bound grants — an approval can never authorize a different action, and (being unpersisted) a crash/restart always requires a fresh approval regardless of what durable state says. `QCoreRuntime.resolveApproval` / `QAgent.approve` are the tested programmatic entry points.
- **Approval UI bridge**: the live notch/panel clarification HUD (`PaceTurnHUDState`, `PaceTurnHUDView`) now resolves real Q approvals. `QRuntimeUISnapshot.pendingApproval` reconstructs the exact `QApprovalRequest` a halt recorded (same deterministic execution-identity-derived id `QApprovalCoordinator` holds — display-only, carries no authority itself), `PaceTurnHUDState.qApprovalRequest(_:)` renders risk-aware Level 2/3 copy, and `CompanionManager.resolveQPermissionApproval`/`resolveClarification` call `QAgent.approve` for real — replacing a pre-2E standing-capability-grant mechanism that could never actually resume a halted task.
- **Real screen perception**: `QBridgeScreenCapture` performs genuine `ScreenCaptureKit` capture (`SCScreenshotManager.captureImage`, the same one-shot API Pace's own `CompanionScreenCaptureUtility` already uses) and `QBridgeVision` performs genuine on-device `Vision.framework` text recognition (`VNRecognizeTextRequest`, `.accurate`) — both are real as of Phase 2G, no longer the Phase 2B fabricated stub. Screen Recording TCC permission is checked read-only (`CGPreflightScreenCaptureAccess`, never requested/manipulated programmatically) and every capture/recognition failure mode fails closed with a distinct, deterministic `QScreenCaptureError` rather than ever fabricating a result.
- **Screen-derived content audit boundary**: the path real screen content takes is safe by construction. `QPlanExecutor` redacts screen/perception-derived (`toolFamily == "perception"`, matching the same predicate already used for `.untrustedScreen` taint) action-result summaries via the existing `QSecretRedactor` before they can reach `QPlanStepResult`, and `QDurablePlanStepSnapshot.init(from:)` applies an unconditional redaction backstop to every step's `resultSummary`/`verifiedEvidence`/failure-reason text at the sole entry point into durable persistence — so no code path can write unredacted screen content into `QDurableTaskStore`. The on-device-only taint propagation into `QTaskContext` (which the local planner needs for correct reasoning) deliberately still carries the raw, unredacted content — it is never logged or persisted, and redaction is never treated as authorization or as removing taint. Full detail: [`../PHASE_2G_REAL_SCREEN_OCR.md`](../PHASE_2G_REAL_SCREEN_OCR.md), [`../PHASE_2G_SECURITY_REMEDIATION.md`](../PHASE_2G_SECURITY_REMEDIATION.md), [`../PHASE_2F_APPROVAL_UI_BRIDGE.md`](../PHASE_2F_APPROVAL_UI_BRIDGE.md), [`../PHASE_2E_CONTROLLED_ACTIONS.md`](../PHASE_2E_CONTROLLED_ACTIONS.md) (and the preceding [`PHASE_2B`](../PHASE_2B_REAL_LOCAL_INTELLIGENCE.md)/[`PHASE_2C`](../PHASE_2C_AUTONOMOUS_CLOSED_LOOP.md)/[`PHASE_2D`](../PHASE_2D_PRODUCTION_RELIABILITY.md) reports).
- **Semantic AXUIElement click (`ui.click_element`, Level 2)**: Q's first controlled UI-interaction capability — a reversible click on a single, semantically-identified Accessibility element (role + `AXIdentifier`, or role + title falling back to description), never screen coordinates, never a CGEvent fallback. `QBridgeAccessibility` (extended in `QBridgeAdapters.swift`) runs a bounded, Q-owned AX tree walk (max depth 12, 3,000 nodes, 1.5s budget) — a fresh implementation, not a reuse of Pace's coordinate-anchored `PaceAXTargeter`/`PaceAXScreenReader` — captures an observation-binding snapshot at resolution, re-verifies the same element reference immediately before dispatch (failing closed on any drift, ambiguity, or disappearance), and presses via `AXUIElementPerformAction`. Flows through the unmodified `QResourceGuard → QPermissionGate → QApprovalCoordinator → QPlanExecutor → QExecutionService` pipeline like every other capability. A new `QVerificationStrategy.axElementStateChanged` re-resolves the same target after the press and diffs its AX state against the pre-click snapshot — a successful press alone is never treated as goal success; verification returns `.failed` (never fabricates `.verified`) when no state change is observed. Accessibility (AX) TCC permission is checked read-only (`AXIsProcessTrusted()`, mirroring `CGPreflightScreenCaptureAccess`'s pattern), and every AX-dependent test branches on its live value rather than assuming either state. Full detail: [`../PHASE_2H_SEMANTIC_CLICK.md`](../PHASE_2H_SEMANTIC_CLICK.md).
- **Semantic AX text entry (`ui.set_text_value`, Level 2)**: Q's second controlled UI-interaction capability — a reversible write of a single, semantically-identified, currently-focused Accessibility text field's value, restricted to an explicit `AXTextField`/`AXTextArea` role allowlist (`QAXTextEntryRolePolicy` — never `AXSecureTextField`, never an unrecognized role) and requiring the target to already be the system's genuinely focused element before any write. `QBridgeAccessibility.setTextValue` (`QBridgeAdapters.swift`) reuses `ui.click_element`'s bounded AX walk and observation-binding re-verify, adds the role/focus checks, treats an already-equal value as an idempotent no-op, and mutates only via `AXUIElementSetAttributeValue(kAXValueAttribute)` — never CGEvent, keyboard simulation, or Return/Tab/submit. The literal value never crosses a persisted or displayed boundary: `QAXTextValueMutationOutcome` and `QSafeTextEntryVerificationEvidence` (`QTextEntrySecurityContracts.swift`) carry only lengths, non-secret target identity, and SHA-256 hashes; `QDurablePlanStepSnapshot.init(from:)` masks the `value` argument via `QSensitiveArgumentPolicy` before durable persistence; and the four places a live, HUD-facing `QApprovalRequest.expectedEffect` is built (`QPlanExecutor`, `QCoreRuntime`'s halt-handling ×2, `QRuntimeUISnapshot.pendingApproval`) route through `QSensitiveArgumentPolicy.approvalSafeLiteralAction`, which builds approval text from non-sensitive targeting metadata only. A new `QVerificationStrategy.axTextValueChanged` re-resolves the target after the write and compares only value hashes — unlike click, an unresolvable target after the write is treated as `.failed`, not assumed success. Full detail: [`../PHASE_2I_SEMANTIC_TEXT_ENTRY.md`](../PHASE_2I_SEMANTIC_TEXT_ENTRY.md) (capability) and [`../PHASE_2I_TEXT_ENTRY_SECURITY_REMEDIATION.md`](../PHASE_2I_TEXT_ENTRY_SECURITY_REMEDIATION.md) (privacy pre-check this implementation closes).
- **Semantic AX element value read (`ui.read_element_value`, Level 0)**: Q's third controlled UI-interaction capability, and its first non-mutating one — reads a single, semantically-identified Accessibility element's value (`kAXValueAttribute`, falling back to title/description for roles without a meaningful value), gated by `QAXElementReadRolePolicy` — a fail-closed allowlist, not a denylist, deliberately wider than `ui.set_text_value`'s narrow two-role write allowlist (reading is categorically lower-risk and needs to cover buttons/labels/checkboxes/menus, not just text fields) but still refusing any role — known or unknown — that isn't explicitly listed; `AXSecureTextField` is never listed and is checked first for a distinct diagnostic. `QBridgeAccessibility.readElementValue` (`QBridgeAdapters.swift`) reuses the identical bounded AX walk `ui.click_element`/`ui.set_text_value` already use; a polymorphic reader handles both `String` and boxed-`NSNumber` AX values. The key security decision: this tool is registered under `toolFamily: "perception"` (not `"ui"`) specifically so it inherits `QPlanExecutor`'s existing `isScreenDerivedStep` sanitize-before-persist / raw-for-reasoning boundary — the same one `screen.ocr` already relies on — with zero changes to `QPlanExecutor` itself; the read value is intentionally exposed to the model (unlike `ui.set_text_value`'s masked input, exposure is this capability's entire purpose) while still being redacted before it reaches audit/durable state/memory. Level 0 means no approval ever halts it, and no dedicated `QVerificationStrategy` was added — a read's own success is its evidence, falling through to the existing default verification path every other read-only tool already uses. Full detail: [`../PHASE_2J_SEMANTIC_ELEMENT_READ.md`](../PHASE_2J_SEMANTIC_ELEMENT_READ.md).
- **Semantic AX element state change (`ui.set_element_state`, Level 2)**: Q's fourth controlled UI-interaction capability — sets a checkbox/radio-button-shaped Accessibility element to an explicit desired on/off state, restricted to `QAXElementStateRolePolicy`'s narrow two-role allowlist (`AXCheckBox`/`AXRadioButton` only, mirroring `ui.set_text_value`'s write-side discipline rather than `ui.read_element_value`'s much wider read-side one). Closes a real gap `ui.click_element` cannot: `QAXElementSnapshot` (click's verification basis) has no field for `kAXValueAttribute`, so pressing a checkbox via `ui.click_element` dispatches correctly but its closed-loop verification will very likely report a false failure, since the one thing that actually changed — the value — is invisible to that check. `QBridgeAccessibility.setElementState` (`QBridgeAdapters.swift`) reuses click's bounded AX walk and identity observation-binding re-verify, and adds a VALUE-drift staleness check neither click nor text-entry needed (state read once at resolution, once again immediately before dispatch, refused on mismatch) plus an idempotency check (already-desired state is a no-op, never pressed). Mutation is `AXUIElementPerformAction(kAXPressAction)` only — the same primitive `ui.click_element` uses, deliberately never `AXUIElementSetAttributeValue`, since many native controls only run their real state-change handling in response to a genuine press. `AXRadioButton` deselection (`desiredState` "off" on an already-"on" radio button) is refused outright — AX provides no reliable way to deselect a single radio button via its own press, only to select a different one in its group. A new `QVerificationStrategy.axElementStateMatchesDesired` re-resolves the target after the press and compares only a SHA-256 hash of its state against the desired state's hash — like text-entry, an unresolvable target after the change is `.failed`, never assumed success. Unlike `ui.set_text_value`, no masking apparatus was needed: `desiredState` is a small, non-sensitive "on"/"off" enum, not free text, so this tool carries no `QSensitiveArgumentPolicy` entry and is registered under `toolFamily: "ui"` (not `"perception"`) since it never observes or exposes arbitrary UI content. Full detail: [`../PHASE_2K_SEMANTIC_ELEMENT_STATE.md`](../PHASE_2K_SEMANTIC_ELEMENT_STATE.md).
- **Semantic menu-bar item selection (`ui.select_menu_item`, Level 2)**: Q's fifth controlled UI-interaction capability — opens one top-level application menu and selects one direct item within it, scoped to exactly one level (never nested submenus, never context/right-click menus, never the Apple menu, never the application's own root menu — the menu bar's index-0 item, explicitly refused). Exists because two separately-approved `ui.click_element` presses cannot reliably open-then-select a menu: the approval HUD appearing between them is itself a focus-stealing event, and native menus dismiss on focus loss — this capability performs both `AXUIElementPerformAction(kAXPressAction)` presses atomically inside one already-approved execution. `QBridgeAccessibility.selectMenuItem` (`QBridgeAdapters.swift`) reads `kAXMenuBarAttribute` off the app element (new AX territory for this codebase — every prior capability walks the app element's general children, not the distinct menu-bar subtree) and, because opening a menu is asynchronous relative to the press returning, bounded-polls for the target item to appear as a direct child of the opened menu — a fixed, hard ceiling (`maxMenuOpenPollAttempts = 10` × `menuOpenPollIntervalNanoseconds = 50ms` = 500ms total, never unbounded), the first wait-based pattern in this codebase's AX capabilities. A new `QVerificationStrategy.axMenuItemSelectionEvidence` distinguishes three re-observed outcomes (`QMenuItemSelectionEvidence`) without inventing a stronger claim than AX can generically provide: the item becoming unresolvable (menu closed) is the expected post-selection lifecycle and is `.verified`; the item remaining resolvable and unchanged, or the application/menu-bar-item itself disappearing unexpectedly, is `.failed`. Unlike `ui.set_element_state`, this capability has no idempotent no-op path — a resumed execution after a crash is always a fresh, independently-authorized attempt (single-use `QApprovalCoordinator` grants make this safe by construction), since "already selected" is not a coherent concept for a momentary command. No masking apparatus needed (non-sensitive menu/item titles); registered under `toolFamily: "ui"`. Full detail: [`../PHASE_2L_SEMANTIC_MENU_SELECTION.md`](../PHASE_2L_SEMANTIC_MENU_SELECTION.md).
- **Semantic slider/stepper value change (`ui.set_slider_value`, Level 2)**: Q's sixth controlled UI-interaction capability — sets an `AXSlider`/`AXStepper`'s numeric value to an explicit `desiredValue`, restricted to `QAXSliderRolePolicy`'s narrow two-role allowlist. Mutation is `AXUIElementSetAttributeValue(kAXValueAttribute)` directly (never a press) — correct here, unlike `ui.set_element_state`'s checkbox/radio press requirement, because a slider/stepper's `AXValue` IS its authoritative state, the same reasoning `ui.set_text_value` already relies on. `desiredValue` is validated against the target's own reported `kAXMinValueAttribute`/`kAXMaxValueAttribute` (new AX territory for this codebase) using STRICT, non-tolerant comparison — a hard security boundary, refused before any mutation and never widened by the numeric tolerance used elsewhere. That tolerance (`QBridgeAccessibility.sliderValuesAreEqual` — a scale-aware hybrid absolute/relative rule, `1e-6` absolute / `1e-9` relative) is the single function reused consistently for idempotency, value/range-drift re-checks (which now cover range bounds, not just current value — reusing Phase 2K's `valueDriftDetected` error case), and closed-loop verification. A new `QVerificationStrategy.axSliderValueMatchesDesired` re-resolves the target and compares its value directly against `desiredValue` — the strongest, most exact verification of any capability shipped so far, since (unlike text-entry/state-change) a numeric value is not sensitive content and needs no hash-based obfuscation; an unresolvable target or inconsistent post-change range is `.failed`, never assumed successful. Idempotent (already-at-value is a no-op), mirroring `ui.set_element_state`'s model. Registered under `toolFamily: "ui"`. Full detail: [`../PHASE_2M_SEMANTIC_SLIDER_VALUE.md`](../PHASE_2M_SEMANTIC_SLIDER_VALUE.md).
- **Semantic application activation (`ui.activate_application`, Level 1)**: Q's seventh controlled UI-adjacent capability, and its first at Level 1 (no approval gate) since `ui.open_app`/`app.quit` — activates one already-running application, matched by an EXACT `localizedName` (never substring/prefix/suffix/fuzzy/case-insensitive), via `NSRunningApplication.activate()` only. Deliberately application-level, not window-level, and deliberately independent of Accessibility permission — no `AXUIElement`/`AXIsProcessTrusted()` call exists anywhere in its implementation, unlike every capability from Phase 2H onward, which is the entire reason it needs no approval gate: `QPermissionGate.evaluate` already routes Level 0/1 straight to `.allow` by policy, so registering the correct risk level is sufficient — no special-cased bypass was added. Resolution fails closed on a missing/empty/whitespace-only name, zero exact matches (`APP_NOT_RUNNING`), or more than one exact match (`APP_AMBIGUOUS_MATCH`, refused rather than guessed). Idempotent by the resolved target's stable `processIdentifier` (not `localizedName`, which a same-named replacement process could otherwise satisfy) — an already-frontmost target makes no `activate()` call at all. A new `QVerificationStrategy.processIsFrontmost` independently re-queries `NSWorkspace.shared.frontmostApplication` and compares its pid against the resolved target's — a successful `activate()` call is never itself treated as proof of success. `QTaskRecoveryManager` gained an observation-first recovery branch mirroring `ui.open_app`'s pattern: already-frontmost is recognized as complete without replay; anything else resets to pending for one fresh, freshly-authorized retry. Registered under `toolFamily: "app"`; `QBridgeAdapters.swift` was not touched. Full detail, including an honest real-hardware-validation gap discovered for the frontmost-transition path in isolated/headless test runners: [`../PHASE_2N_APPLICATION_ACTIVATION.md`](../PHASE_2N_APPLICATION_ACTIVATION.md).
- **Semantic AX element focus (`ui.focus_element`, Level 2)**: Q's eighth controlled UI-interaction capability — requests Accessibility keyboard focus for a single semantically-identified element via `AXUIElementSetAttributeValue(kAXFocusedAttribute)` only, restricted to `QAXFocusableRolePolicy`'s narrow fail-closed allowlist (`AXButton`, `AXCheckBox`, `AXRadioButton`, `AXTextField`, `AXTextArea`, `AXSlider`, `AXStepper` — the union of every role already proven interactive by an existing write-side policy, plus `AXButton`; `AXSecureTextField`/`AXStaticText`/`AXImage`/`AXGroup`/`AXPopUpButton`/`AXComboBox` are all deliberately excluded). Unlike `ui.activate_application`'s application-level Level 1 exception, focus-setting is element-level AX interaction and follows the Level 2 precedent every other per-element AX write already established. Closes a gap explicitly named in `ui.set_text_value`'s own contract since Phase 2I: that tool "never clicks/focuses a field itself" and requires the target to already be focused. Idempotent by the systemwide `kAXFocusedUIElementAttribute` element's identity (`CFEqual`, the same read primitive `ui.set_text_value`'s focus precondition check already uses) — an already-focused target makes no `AXUIElementSetAttributeValue` call at all. A new `QVerificationStrategy.axElementIsFocused` independently re-resolves the target and re-reads focus state fresh — a successful mutation call is never itself treated as proof of success. `QTaskRecoveryManager` gained an observation-first recovery branch reusing the identical independent observation primitive verification uses (no parallel resolver): already-focused is recognized complete without replay; anything else resets to pending, requiring both a fresh `QExecutionIdentity` and a fresh approval grant. Never reads element values — structurally cannot expose secure-field content, unlike `ui.read_element_value`/`ui.set_text_value`. Registered under `toolFamily: "ui"`. Full detail, including a real-AX-E2E-blocked-by-Accessibility-trust finding consistent with Phase 2H–2M: [`../PHASE_2O_SEMANTIC_ELEMENT_FOCUS.md`](../PHASE_2O_SEMANTIC_ELEMENT_FOCUS.md).
- **Semantic popup item selection (`ui.select_popup_item`, Level 2)**: Q's ninth controlled UI-interaction capability — resolves a single `AXPopUpButton` (`QAXPopupRolePolicy`'s only allowed role; `AXComboBox` is deliberately never allowed) and, unless it already shows the desired item, opens it and selects one direct `AXMenuItem` within its opened `AXMenu` atomically within one approved execution — the exact same two-press-atomic-with-bounded-poll mechanism `ui.select_menu_item` (Phase 2L) already proved works in this codebase, reused verbatim rather than duplicated with new constants. Closes the one common AppKit form-control interaction that had no reachable path at all before this phase: `ui.click_element` applies no role allowlist and can already press a popup open, but cannot reliably open-then-select a specific item across two separately-approved steps, for the identical reason `ui.select_menu_item` needed its own capability. A genuine improvement over `ui.select_menu_item`'s evidence model: unlike a momentary menu-bar command, an `AXPopUpButton` is a persistent value-holding control whose `kAXValueAttribute` is already readable (`QAXElementReadRolePolicy` has listed it since Phase 2J) — so both idempotency (before dispatch) and closed-loop verification (`QVerificationStrategy.axPopupValueMatchesDesired`, after dispatch) compare the popup's own current value directly against the requested item title, a stronger, more direct signal than `axMenuItemSelectionEvidence`'s indirect "item disappeared" heuristic. `QTaskRecoveryManager` gained an observation-first recovery branch reusing that identical independent observation primitive (no parallel resolver): already-showing-the-requested-item is recognized complete without replay; anything else resets to pending, requiring both a fresh `QExecutionIdentity` and a fresh approval grant. Registered under `toolFamily: "ui"`. Full detail, including a real-AX-E2E-blocked-by-Accessibility-trust finding consistent with Phase 2H–2O: [`../PHASE_2P_SEMANTIC_POPUP_SELECTION.md`](../PHASE_2P_SEMANTIC_POPUP_SELECTION.md).
- **Semantic disclosure triangle toggle (`ui.toggle_disclosure`, Level 2)**: Q's tenth controlled UI-interaction capability — requests an explicit desired expand/collapse state ("expanded" or "collapsed", never a blind toggle whose result is unknown) for exactly one semantically-identified `AXDisclosureTriangle`, restricted to `QAXDisclosureRolePolicy`'s single-role fail-closed allowlist (`AXButton`/`AXCheckBox`/`AXRadioButton`/`AXPopUpButton`/`AXComboBox`/`AXGroup`/`AXOutline` are all deliberately excluded — this capability is never broadened to a generic press-based toggle). Mutation is `AXUIElementPerformAction(kAXPressAction)` only — the same primitive `ui.set_element_state`/`ui.click_element` already use, correct here for the identical reason: a disclosure triangle only runs its real expand/collapse handling in response to a genuine press, not a raw `kAXValueAttribute` write. Structurally the same interaction shape `ui.set_element_state` (Phase 2K) already established for checkbox/radio's binary state, applied to a role already read-allowlisted since Phase 2J — state reading reuses the identical 0/1 `kAXValueAttribute` interpretation discipline `axCheckboxRadioState` established, never guessing at an indeterminate value. Idempotent by direct value comparison: already-at-the-desired-state is a verified no-op, no press performed. A new `QVerificationStrategy.axDisclosureStateMatchesDesired` independently re-resolves the target and re-reads its state fresh — a successful press is never itself treated as proof of success. `QTaskRecoveryManager` gained an observation-first recovery branch reusing that identical independent primitive: already-at-the-desired-state is recognized complete without replay; anything else resets to pending, requiring both a fresh `QExecutionIdentity` and a fresh approval grant. An empirical fixture-construction finding during implementation established that `NSOutlineView`'s own row disclosure control cannot be tagged with a settable `AXIdentifier` and so cannot serve as this capability's real-fixture test target; a custom `NSAccessibility`-role-overriding fixture was used instead — a genuinely real, live AXUIElement, not a simulation — without weakening the production role policy. Registered under `toolFamily: "ui"`. Full detail, including a real-AX-E2E-blocked-by-Accessibility-trust finding consistent with Phase 2H–2P: [`../PHASE_2Q_SEMANTIC_DISCLOSURE_TOGGLE.md`](../PHASE_2Q_SEMANTIC_DISCLOSURE_TOGGLE.md).
- **Semantic tab selection (`ui.select_tab`, Level 2)**: Q's eleventh controlled UI-interaction capability — requests an explicit desired selection state ("true"/"false" — `desiredSelected`, never a blind toggle) for exactly one semantically-identified tab. **A critical empirical correction discovered mid-implementation**: there is no standalone "AXTab" role anywhere in macOS's Accessibility API (confirmed directly against the AppKit SDK's authoritative `NSAccessibilityConstants.h`, which lists every `NSAccessibilityRole` constant Apple has ever defined) — a tab item's real, header-confirmed shape is base role `AXRadioButton` carrying `kAXSubroleAttribute == "AXTabButton"`. Implementation halted immediately on this finding (a capability scoped to a role that can never match a real element would be a silent production defect, not an honest environment limitation) and was reported to the user, who approved retargeting to the real shape. `QAXTabRolePolicy` therefore allows exactly `AXRadioButton`, but `selectTab` additionally, unconditionally requires the `AXTabButton` subrole before ever treating a resolved element as a tab — a generic `AXRadioButton` lacking that subrole is refused (`targetNotATabButton`), never silently accepted, which is what keeps this capability from being cross-wired with `ui.set_element_state`'s existing, unconditional `AXRadioButton` coverage (the two capabilities read entirely different attributes for their respective state models: `kAXSelectedAttribute` here, `kAXValueAttribute` there). Mutation is `AXUIElementPerformAction(kAXPressAction)` only — the same primitive `ui.toggle_disclosure`/`ui.set_element_state`/`ui.click_element` already use. Idempotent: already-at-the-desired-selection-state is a verified no-op, no press performed. A new `QVerificationStrategy.axTabSelectionMatchesDesired` independently re-resolves the target (re-verifying both role and subrole) and re-reads its selection state fresh. `QTaskRecoveryManager` gained a matching observation-first recovery branch. Registered under `toolFamily: "ui"`. Full detail, including the honest empirical-correction narrative and a real-AX-E2E-blocked-by-Accessibility-trust finding consistent with Phase 2H–2Q: [`../PHASE_2R_SEMANTIC_TAB_SELECTION.md`](../PHASE_2R_SEMANTIC_TAB_SELECTION.md).
- **Semantic table row selection (`ui.select_table_row`, Level 2)**: Q's twelfth controlled UI-interaction capability — requests selection (never deselection) of exactly one semantically-identified table row. Confirmed directly against the authoritative `AXRoleConstants.h`/`AXAttributeConstants.h` (the C-string-macro headers underlying Phase 2R's `NSAccessibilityConstants.h` finding): `AXRow` is a genuine, standalone base role (`QAXTableRowRolePolicy`'s only allowed role), distinct from the subrole-borrowing shape a tab item uses. `selectTableRow` additionally, unconditionally requires BOTH the `AXTableRow` subrole AND a resolved `kAXParentAttribute` whose own role is exactly `AXTable` — a row lacking either is refused (`targetNotATableRow`/`tableContextUnavailable`), never accepted merely because it looks like a table row. `AXOutlineRow` — a real, SDK-confirmed subrole `NSOutlineView` rows carry instead — is recognized but explicitly, distinctly refused (`outlineRowUnsupported`), deferred to a future phase rather than assumed compatible. Unlike every prior explicit-desired-state capability, this phase supports selection only: `desiredSelected` MUST be `true`, refused (`rowDeselectionUnsupported`) before any Accessibility Trust check or application resolution is even attempted if `false` — never a blind toggle. Mutation is `AXUIElementPerformAction(kAXPressAction)` only — the same primitive `ui.select_tab`/`ui.toggle_disclosure`/`ui.set_element_state`/`ui.click_element` already use. Authoritative selection state is `kAXSelectedAttribute` — the identical attribute already proven correct for `ui.select_tab`, deliberately never `kAXSelectedRowsAttribute` (the table-level multi-selection array). Idempotent: already-selected is a verified no-op, no press performed. A new `QVerificationStrategy.axTableRowSelectionMatchesDesired` independently re-resolves the target (re-verifying role, subrole, AND table-parent context) and re-reads its selection state fresh. `QTaskRecoveryManager` gained a matching observation-first recovery branch. Multi-row/range selection and cell-level (`AXCell`) addressing are structurally out of scope — the schema's flat `[String: String]?` parameter shape cannot express an array or range. Registered under `toolFamily: "ui"`. Full detail, including the honest two-level-AX-bridging validation gap and a real-AX-E2E-blocked-by-Accessibility-trust finding consistent with Phase 2H–2R: [`../PHASE_2S_SEMANTIC_TABLE_ROW_SELECTION.md`](../PHASE_2S_SEMANTIC_TABLE_ROW_SELECTION.md).
- **Semantic outline row selection (`ui.select_outline_row`, Level 2)**: Q's thirteenth controlled UI-interaction capability — the reciprocal of `ui.select_table_row`, targeting `NSOutlineView`-shaped trees (Finder sidebar, Xcode navigator, Mail account tree, System Settings sidebar) instead of flat tables. Confirmed directly against the authoritative `AXRoleConstants.h`: `AXRow` is the identical base role table rows use (`QAXOutlineRowRolePolicy`'s only allowed role) — outline and table rows share one base role, differing only by subrole. `selectOutlineRow` additionally, unconditionally requires BOTH the `AXOutlineRow` subrole AND a resolved `kAXParentAttribute` whose own role is exactly `AXOutline` — a row lacking either is refused (`targetNotAnOutlineRow`/`outlineContextUnavailable`), never accepted merely because it looks like an outline row. `AXTableRow` — the real subrole `ui.select_table_row` already owns — is recognized but explicitly, distinctly refused (`tableRowUnsupportedForOutline`), the exact mirror of `ui.select_table_row`'s own `AXOutlineRow` refusal (Phase 2S). Selection only: `desiredSelected` MUST be `true`, refused (`outlineRowDeselectionUnsupported`) before any Accessibility Trust check or application resolution is even attempted if `false`. No auto-expand-then-select: resolution reuses the existing bounded tree walk unmodified, never reading/writing `kAXDisclosingAttribute` or performing a disclosure action — a collapsed row's descendant is simply unresolvable, never automatically expanded. Mutation is `AXUIElementPerformAction(kAXPressAction)` only — the same primitive `ui.select_table_row`/`ui.select_tab`/`ui.toggle_disclosure`/`ui.set_element_state`/`ui.click_element` already use; `kAXSelectedAttribute` is read-only in this implementation. Idempotent: already-selected is a verified no-op, no press performed. A new `QVerificationStrategy.axOutlineRowSelectionMatchesDesired` independently re-resolves the target (re-verifying role, subrole, AND outline-parent context) and re-reads its selection state fresh. `QTaskRecoveryManager` gained a matching observation-first recovery branch. Registered under `toolFamily: "ui"`. Full detail, including the honest two-level-AX-bridging validation gap (inherited and extended from Phase 2S) and a real-AX-E2E-blocked-by-Accessibility-trust finding consistent with Phase 2H–2S: [`../PHASE_2T_SEMANTIC_OUTLINE_ROW_SELECTION.md`](../PHASE_2T_SEMANTIC_OUTLINE_ROW_SELECTION.md).
- **Semantic window minimized state (`ui.set_window_minimized`, Level 2)**: Q's fourteenth controlled UI-interaction capability, and its first at the WINDOW level — every prior capability targets a control inside a window, never the window itself. Confirmed directly against the authoritative `AXAttributeConstants.h`: `kAXMinimizedAttribute` is documented "Whether a window is currently minimized to the dock... Writable? Yes." — a directly-settable boolean, so mutation is `AXUIElementSetAttributeValue(kAXMinimizedAttribute)` only, never `AXUIElementPerformAction` (unlike every row/tab/disclosure capability). Scoped to `QAXWindowRolePolicy`'s single-role allowlist (`AXWindow` only). Deliberately NOT used: `kAXRaiseAction` — a real, defined AX action whose Apple header ships with an entirely empty `@discussion` block (no documented behavior exists for it) — and the read-only `kAXMinimizeButtonAttribute` convenience reference. Genuinely bidirectional, unlike `ui.select_table_row`/`ui.select_outline_row`'s selection-only restriction: both `desiredMinimized=true` and `=false` are fully supported, symmetric, idempotent target states. Never activates, focuses, or raises the target application/window as a side effect — `AXUIElementCreateApplication` is a pure object-reference constructor with no such effect. Idempotent in either direction: already-at-the-desired-state is a verified no-op, no attribute write performed. A new `QVerificationStrategy.axWindowMinimizedStateMatchesDesired` independently re-resolves the target and re-reads `kAXMinimizedAttribute` fresh — state is never inferred from window position, visibility, frontmost state, Dock appearance, or title. `QTaskRecoveryManager` gained a matching observation-first, bidirectional recovery branch. Registered under `toolFamily: "ui"`. A real `NSWindow` fixture requires no custom `NSAccessibility`-role override at all (unlike every Phase 2S/2T fixture) — it is already a genuine `AXWindow` element by default AppKit bridging. Full detail, including a real-AX-E2E-blocked-by-Accessibility-trust finding consistent with Phase 2H–2T: [`../PHASE_2U_SEMANTIC_WINDOW_MINIMIZED_STATE.md`](../PHASE_2U_SEMANTIC_WINDOW_MINIMIZED_STATE.md).
- **Semantic application hidden state (`ui.set_application_hidden`, Level 2)**: Q's fifteenth controlled UI-interaction capability, and its second (after `ui.activate_application`, Phase 2N) to deliberately bypass `AXUIElement` entirely in favor of native AppKit: mutation is `NSRunningApplication.hide()`/`.unhide()` only, state is read via `.isHidden` — never `AXUIElement`, never gated on `AXIsProcessTrusted()`. Resolved by an EXACT `localizedName` match, identical discipline to `ui.activate_application`'s own resolution (zero matches → `APP_NOT_RUNNING`, more than one exact match → `APP_AMBIGUOUS_MATCH`, never guessed). Unlike `ui.activate_application`'s Level 1 classification, this remains Level 2: hiding affects every window of the target application simultaneously, a broader blast radius than a single-window mutation. Genuinely bidirectional, like `ui.set_window_minimized`: both `desiredHidden=true` and `=false` are fully supported, symmetric, idempotent target states — read-before-mutate against `isHidden`, no method call made if already at the desired state. A new `QVerificationStrategy.applicationHiddenStateMatchesDesired` independently re-resolves the application by its stable `processIdentifier` (never `localizedName` again) and re-reads `isHidden` fresh — structurally the same pid-anchored model `.processIsFrontmost` already established. `QTaskRecoveryManager` gained a matching observation-first, bidirectional recovery branch (using a fresh name resolution, since a persisted pid is not available at recovery time — the same discipline `ui.activate_application`'s own recovery branch already uses). Registered under `toolFamily: "app"` — no new provenance taxonomy invented. `QBridgeAdapters.swift` was not touched — the first UI-family capability requiring zero AX-bridge changes. An honestly investigated session limitation was found and documented during implementation: `NSRunningApplication.hide()`/`.unhide()` were confirmed (via an out-of-process probe independent of the test harness) to be rejected by the OS when called from a process with no real `NSApplication` activation-policy context in this isolated test-runner session — a calling-process limitation, not a defect, since the capability's own independent verification correctly reports `.failed` rather than fabricating success whenever the underlying OS call does not actually change state. Full detail: [`../PHASE_2V_SEMANTIC_APPLICATION_HIDDEN_STATE.md`](../PHASE_2V_SEMANTIC_APPLICATION_HIDDEN_STATE.md).
- **Semantic scroll position (`ui.set_scroll_position`, Level 2)**: Q's sixteenth controlled UI-interaction capability — sets the ABSOLUTE numeric position of exactly one semantically-identified scroll bar; never scroll-by-delta, never scroll-to-visible, never scroll-wheel/mouse/keyboard/coordinate simulation. Confirmed directly against the authoritative `AXAttributeConstants.h`: `kAXValueAttribute`'s own discussion block explicitly names scroll bars by name ("a kAXScrollBar's kAXValueAttribute is writable because it allows an efficient way for the user to get to a specific position"), and `kAXMinValueAttribute`/`kAXMaxValueAttribute`'s own discussion blocks explicitly name "sliders and scroll bars" together — the same range-bound pattern `ui.set_slider_value` already established and this capability reuses VERBATIM, including its exact public `sliderValuesAreEqual` tolerance function, rather than duplicating a subtly different rule. The target scroll bar is never searched for directly (raw `AXScrollBar` elements are commonly unlabeled) — resolution anchors on the containing `AXScrollArea` (`QAXScrollAreaRolePolicy`'s only allowed role, resolved via the unmodified exact-match resolver every capability shares) plus an explicit, never-inferred `orientation` parameter, then follows the documented read-only convenience-reference attribute (`kAXHorizontalScrollBarAttribute`/`kAXVerticalScrollBarAttribute` — resolution only, NEVER mutated) to the actual scroll bar, whose own `kAXRoleAttribute` is independently re-validated as exactly `AXScrollBar` before ever being treated as genuine. Mutation is `AXUIElementSetAttributeValue(kAXValueAttribute)` only — never `kAXIncrementAction`/`kAXDecrementAction`/`kAXPressAction`. Idempotent: already-at-the-desired-position (within tolerance) is a verified no-op. A new `QVerificationStrategy.scrollPositionMatchesDesired` independently re-resolves the ENTIRE identity chain fresh and re-reads `kAXValueAttribute`. `QTaskRecoveryManager` gained a genuine observation-first recovery branch — notably, sibling `ui.set_slider_value` itself has none (a pre-existing gap left untouched), but this phase's own explicit recovery requirement is honored with a real implementation. Registered under `toolFamily: "ui"`. A real `NSScrollView` fixture requires no custom `NSAccessibility` role override for the scroll area itself, the same favorable position `ui.set_slider_value`'s `NSSlider`/`ui.set_window_minimized`'s `NSWindow` already enjoy. Full detail, including the open hardware-validation question of whether real scroll areas expose the convenience-reference attributes when scrolling is available: [`../PHASE_2W_SEMANTIC_SCROLL_POSITION.md`](../PHASE_2W_SEMANTIC_SCROLL_POSITION.md).
- **Semantic window main designation (`ui.set_window_main`, Level 2)**: Q's seventeenth controlled UI-interaction capability. Confirmed directly against the authoritative `AXAttributeConstants.h`: `kAXMainAttribute` is documented "Whether a window is the main document window of an application... Main does not necessarily imply that the window has key focus... Writable? Yes." SELECT-ONLY, by direct analogy to `ui.select_tab`/`ui.select_outline_row`/`ui.select_table_row`: `desiredMain` must be `true`; `desiredMain=false` is refused deterministically — in both `QBridgeAccessibility.setWindowMain` and `QExecutionService.executeSetWindowMain` — before any Accessibility Trust check or AX call of any kind, never implemented as a blind toggle. Reuses `QAXWindowRolePolicy` (Phase 2U) UNMODIFIED — the target role is identically `AXWindow` — and reuses `collectMatches`/`snapshotIfMatches` for exact-match target resolution, exactly like every prior AX mutation capability. Mutation is `AXUIElementSetAttributeValue(kAXMainAttribute, kCFBooleanTrue)` only — never `kAXRaiseAction`, never `kAXFocusedAttribute`, never `NSRunningApplication.activate()`, never any window-ordering call — and this capability makes NO implicit claim about activation, focus, raise, or frontmost-ness, matching `kAXMainAttribute`'s own documented decoupling from key focus; a window that becomes main but stays visually non-frontmost still counts as full success. Idempotent: already-main is a verified no-op, `changeKind: .alreadyDesired` itself the structural proof no `AXUIElementSetAttributeValue` call occurred. A new `QVerificationStrategy.windowMainStateMatchesDesired` independently re-resolves the target fresh and re-reads `kAXMainAttribute`. `QTaskRecoveryManager` gained a matching observation-first recovery branch (single-direction, since the contract guarantees `desiredMain` is always `true`), reusing the same `observeWindowMainEvidence` primitive verification uses. Explicitly forbids any agent-side exclusivity-enforcement algorithm: never enumerates sibling windows, never clears `kAXMainAttribute` on any other window, never selects multiple windows — `kAXMainAttribute` exclusivity semantics are owned entirely by the OS/application. Registered under `toolFamily: "ui"`. A real `NSWindow` fixture requires no custom `NSAccessibility`-role override, the same favorable position `ui.set_window_minimized`'s fixture already enjoys. Full detail, including a two-window exclusivity OBSERVATION test (mechanism implemented, empirical capture blocked in this session by `AXIsProcessTrusted() == false`, honestly reported rather than fabricated): [`../PHASE_2X_SEMANTIC_WINDOW_MAIN_DESIGNATION.md`](../PHASE_2X_SEMANTIC_WINDOW_MAIN_DESIGNATION.md).
- **Semantic window close (`ui.close_window`, Level 3 — HIGH RISK)**: Q's eighteenth controlled UI-interaction capability, and its FIRST genuinely one-way, irreversible semantic UI capability — every prior capability (Phase 2N–2X) is a reversible boolean/value write or selection. Confirmed directly against the authoritative `AXAttributeConstants.h`: `kAXCloseButtonAttribute` is documented "A convenience attribute so assistive apps can quickly access a window's close button element... Writable? No." — a read-only convenience reference to the actual close-button `AXButton` element, which is then pressed via `AXUIElementPerformAction(kAXPressAction)` — never `AXUIElementSetAttributeValue` (no writable "closed" attribute exists), never `kAXCloseAction` (no such window action exists). Classified `.level3HighRisk` — the same tier as `app.quit`, whose blast radius this strictly narrows (one window, never the whole application) but whose data-loss risk is comparably real; deliberately NOT downgraded to Level 2. Reuses `QAXWindowRolePolicy` (Phase 2U) unmodified for the window search criterion; the close-button reference's own role is independently re-validated as exactly `AXButton` before ever being pressed, by direct analogy to `ui.set_scroll_position`'s `targetNotAScrollBar` check. Introduces ABSENCE-BASED verification — a first for this codebase: `QVerificationStrategy.windowCloseVerified` succeeds ONLY when the owning application is independently confirmed still running AND the exact original window identity no longer resolves; application termination is NEVER credited as a successful window close (a dedicated `.applicationNotRunning` evidence case, distinct from `.windowStillPresent`/`.ambiguousTarget`/`.permissionUnavailable`, all of which fail closed rather than being folded into absence). Idempotent in one direction only: a window already absent (application confirmed running) is a no-op success; an ambiguous, inaccessible, or permission-denied resolution is NEVER converted into idempotent absence. `QTaskRecoveryManager` gained a matching observation-first recovery branch reusing the identical `observeWindowCloseEvidence` primitive — a still-resolvable window is never blindly re-pressed. This capability NEVER interacts with any save/discard dialog the press may cause to appear (no click-Save/Don't-Save/Cancel, no simulated Return/Escape, no dialog content inspection) — the press is the entire mutation, and any resulting dialog is left entirely to the human user; the test suite proxies this scenario honestly via an `NSWindowDelegate.windowShouldClose` block (never a fabricated real save dialog). Never enumerates or closes more than the one exact target; never calls `NSRunningApplication.terminate()` or any `app.quit` code path internally. Registered under `toolFamily: "ui"`. Full detail, including the real-AX-E2E limitation consistent with every phase since 2N: [`../PHASE_2Y_SEMANTIC_WINDOW_CLOSE.md`](../PHASE_2Y_SEMANTIC_WINDOW_CLOSE.md).
- **Semantic window enumeration (`ui.list_windows`, Level 0 — READ-ONLY)**: Q's nineteenth controlled UI-interaction capability, and its FIRST read-only capability at the window layer — no mutation, no approval, no recovery. Enumerates the windows belonging to exactly ONE named, running application via `kAXWindowsAttribute` — a DIRECT CHILD read only (at most five per-element attribute reads: `kAXRoleAttribute`/`kAXTitleAttribute`/`AXIdentifier`/`kAXMinimizedAttribute`/`kAXMainAttribute`), never a recursive descent into any returned window's own descendants — structurally distinct from every semantic-target-resolution capability in this codebase (all of which use the bounded-but-recursive `collectMatches` tree walker). Application identity is resolved by EXACT `localizedName`/`bundleIdentifier` match; more than one running process matching the same name is ambiguous and fails closed (reuses the generic `QAXInteractionError.ambiguousTarget` case). The raw returned collection's size is checked against a defensive maximum (`QBridgeAccessibility.maxWindowEnumerationCount` = 64) BEFORE any per-element read; a value that cannot be read as `[AXUIElement]` is a hard failure (`windowsCollectionMalformed`), while a failed/absent attribute read is a legitimate EMPTY result (e.g. a headless/background-only application). Only `AXWindow`-role elements are included; every other metadata field is independently optional and never fails the whole enumeration. Array ordering is NEVER treated as meaningful — no frontmost/z-order/main-window inference is ever drawn from position. A new `QVerificationStrategy.windowEnumerationSucceeded` deliberately avoids the generic `.customCheck { true }` bypass, checking the execution result's own `success` flag as a genuine assertion instead — there is no independent physical state to re-observe after a stateless read, so the read's own outcome already is the ground truth. This is a POINT-IN-TIME SNAPSHOT ONLY: `QAXWindowMetadata` carries no `AXUIElement` reference of any kind, and this result is never itself an actionable target — every subsequent mutation capability must independently perform its own fresh, exact target resolution and its own approval flow. PRIVACY: this phase's analysis required inspecting the persistence pipeline itself — `QDurablePlanStepSnapshot.init(from:)` reads only `step.result.summary`/`step.result.verifiedEvidence`, never `step.result.outputData` (confirmed by source inspection AND a dedicated empirical test constructing a real step with sensitive titles in `outputData` and asserting none reach the durable snapshot) — so the structured per-window list this capability returns structurally cannot reach durable storage, audit logs, or long-term memory; `QActionResult.summary` is additionally kept to an aggregate window COUNT only, never embedding individual titles, on this capability's own side of that boundary too. Registered under `toolFamily: "ui"`. `QTaskRecoveryManager` gained no new branch — Level 0 reads have no in-flight mutation to recover, matching `system.running_apps`/`ui.read_element_value`/`accessibility.read` exactly. Full detail, including the real-AX-E2E limitation consistent with every phase since 2N: [`../PHASE_2Z_SEMANTIC_WINDOW_ENUMERATION.md`](../PHASE_2Z_SEMANTIC_WINDOW_ENUMERATION.md).
- **Local-only**: zero cloud endpoints; planning backends are Apple Foundation Models, in-process MLX, or loopback-only Ollama/LM Studio, matching the rest of Pace's on-device posture.

## Local-mode setup

See [SETUP_LOCAL.md](https://github.com/HeyPace/pace/blob/main/SETUP_LOCAL.md) for the full recipe. The complete Info.plist switch reference (local VLM / planner / TTS-sidecar / transcription-provider knobs and their defaults) lives in [`../development/info-plist-switches.md`](../development/info-plist-switches.md).
